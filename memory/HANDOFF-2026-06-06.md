# XLMARKET — HANDOFF (2026-06-06 → uus sessioon)

> Täisdetailid: `memory/sessions/2026-06-06-xl.md`. Live: xlmarket.ee (Coolify, 65.21.126.235, app uuid `uo28ovobnflauslqjgxeohl0`).

## 1. 🔴 Cart async-stall — seis + välistused (PRIORITEET 1)

**Sümptom:** POST /store/carts vahelduvalt **5-12s** (medusa http-log duration ~5497ms), ~pooled kiired (0.1s). Mõjutab checkout'i + Montonio makset.

**Stopgap LIVE** (band-aid, töötab): `storefront/lib/medusa-proxy.ts` timeout 3s→12s + `app/api/cart/route.ts` 2×13s retry. → 6/8 õnnestub (1-9s), 504-d kadusid, 2/8 ikka >26s.

**VÄLISTATUD (ära korda):** DNS (lookup <8ms), pg-pool (6 üh, 0 idle-in-txn), redis-churn (2 stabiilset, age 633s), conntrack (1%), base-image (värske alpine+debian node → connect <10ms), autoSelectFamily (`--no-network-family-autoselection` deployed → internalConnect-vead 0), Montonio-link (aeglane ka ilma), mailcow-võrk (lahutamine ei aidanud), host-võrk (coolify-proxy→medusa 0ms), pg/redis OP-d (kiired).

**✅ ROOT CAUSE LEITUD (staging-profiling 2026-06-06 õhtu — vt sessioonilogi tabel):**
Stall = `futex FUTEX_WAIT_PRIVATE` (in-process condvar), MITTE võrk/DNS/timer/GC/CPU-throttle/redis-lock. futex skaleerub cart'idega (10→3276 kõnet idle→load). MEM flat 159MB (pole GC), CPU 24%/12 (pole CPU-bound), `cpu.max:max` (pole throttle). **per-thread: 4× libuv-worker (UV_THREADPOOL_SIZE=4) põleb cart-burstil.** → **Medusa cart-create dispatch'ib CPU-tööd (crypto/fs) vaikimisi 4-threadi libuv threadpool'i; main pargib condvaril.** Kontention: isoleeritud cart stallib ~30%, **6 paralleelset → KÕIK >16s** (pool ammendub).

**JÄRGMINE SAMM (staging, vajab restarti):** 1) **`UV_THREADPOOL_SIZE=64` env** — odav, suur mõju, esimene testida. 2) workflow-engine+locking → redis-provider + paranda staging redis. 3) `--dns-result-order=ipv4first`. Stopgap (12s+retry) jääb kuni UV_THREADPOOL fix deployed. Prod: redis OK → leebem, aga sama threadpool-piir.

## 2. 🟡 Cloudflare — zone pending / proxy ei aktiveeru

- NS delegeeritud ✅ (edward + penny.ns.cloudflare.com, globaalselt levinud).
- Proxy HALL 🟡 — autoriteetne NS (edward/penny) tagastab ikka origin IP **65.21.126.235** (A-rekord "DNS only", mitte "Proxied"). **Kasutaja proovis 3× dashboardis oranžiks, ei jää.**
- **Tõenäoline põhjus:** zone "pending"/vale konto VÕI rekord hüppab tagasi halliks (konflikt). Kontrolli Overview: zone-staatus "Active"? assigned NS = edward+penny?
- Mul POLE CF API-tokenit (puudub). Zone ID antud: `04661523023085812102ba1064d79bd375` (**34 märki — CF zone ID on 32, KONTROLLI ÜLE**).
- **Lahendus:** kasutajalt CF API-token (Zone.DNS:Edit + Zone:Read) → `PATCH /zones/{id}/dns_records/{rec} {proxied:true}` + activation_check. VÕI kasutaja dashboardis kustutab+loob A-rekordi Proxied'ina. **Pole launch-blokeerija** (sait töötab, SSL OK; proxy = CDN/kaitse).

## 3. ⚠️ vevor-to-v3.json l1_defaults VIGA

`backend/src/scripts/vevor-to-v3.json` `l1_defaults` viitab **VANADELE L1-handle'tele** mis ei eksisteeri praeguses 18-L1 taksonoomias:
- `fitness-sports-recreation` → peaks olema `fitness-sports-games`
- `plumbing-water-systems` → PUUDUB (vaja `construction-building` vm)
- `outdoor-power-landscaping`, `electrical-energy` → ei kattu praeguste handle'tega

**Tagajärg:** uued feed-sync tooted neis VEVOR-L1-des **kukuvad läbi l1_default'i → ei kategoriseeru** (sel sessioonil 32 toodet käsitsi parandatud, 6 veel l1_slug NULL). **Vaja l1_defaults sünkida 18 praeguse L1-handle'ga** (vt `node -e "...level===1"` storefront/lib/category-tree.generated.json).

## 4. Järgmised prioriteedid (järjekorras)
1. **Cart async-stall profiling** (P1 — checkout/Montonio sõltub)
2. **Cloudflare proxy** (P2 — CF token / dashboard)
3. **vevor-to-v3.json l1_defaults sünk** (uued tooted ei kategoriseeru muidu)
4. **Feed-sync Coolify Scheduled Task** — skriptid valmis+deployed (`backend/scripts/feed-sync-bulk.sh`), vaja registreerida `0 */4 * * *` + test
5. **Montonio sandbox e2e test** — vt §5
6. **Powermat:** powermat-to-v3.json + pipeline rewire + import
7. **Faas C** (900 kat 1-4 tootega) — PÄRAST Powermati
8. Post-launch: xl-admin ET-tõlge, FAAS 2 live-UI, legacy 629 DB-kat

## 5. Montonio test — VEEL TEGEMATA
- Provider live (`pp_montonio_montonio`), regiooniga lingitud (system_default eemaldatud), hostname fix deployed, webhook URL OK (MEDUSA_BACKEND_URL).
- **E2e brauseri-test tegemata** — sõltub cart-stall'ist (makse aeglane sama põhjusega). Test: korv → checkout → Montonio sandbox → "makse õnnestus" → tagasi /et/tellimus/tagasi → tellimus admin'is + email.
- Sandbox-võtmed env-s (Access ff600802..., Secret 44-märki). MONTONIO_ENV=sandbox.

## Deploy-meelespea (KORDUB)
1. `curl -H "Authorization: Bearer $(cat /tmp/coolify-token.txt)" "http://localhost:8000/api/v1/deploy?uuid=uo28ovobnflauslqjgxeohl0"`
2. Medusa boot ~8.5 min. **Post-steps:** mailcow-võrk connect + Traefik yaml (medusa-admin.yaml + xlmarket-dev.yaml) konteineri-nimed coolify-proxy's. DB/Meili IP-d muutuvad.
