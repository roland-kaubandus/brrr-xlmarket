# Sessioon 2026-06-08 — XL skaleerimine: eeltingimus #1 (meili settings-on-boot)

## Mida tehti

**Eeltingimus #1 — meili settings-on-boot → ühekordseks (commit bc8f8809)**
- `backend/Dockerfile` patch-blokk laiendatud (3. patch): plugin loader
  `@rokmohar/medusa-plugin-meilisearch/.../loaders/index.js` SKIP'ib `updateSettings` boot'il
  kui `SKIP_MEILISEARCH_STARTUP_INDEXING !== 'false'`. Settingsid jäävad SSoT `index-meilisearch.mjs`'i.
- Deploy STAGING (prod PUUTUMATA, afcad7e4). Mõõdetud:
  - Meili 408 boot'il: korduv → **0**
  - Boot: ~514s → **413s** (-100s)
  - Settings terved (searchable 7, filter 13, sort 3, 17105 docs), browse/search OK
  - Boot-aja suur osa (~5.5min) = Medusa core-bootstrap, MITTE meili.

**Multi-instance re-test (meili-fix'iga)**
- medusa-2 manuaalne instance ÕIGEL võrgul (`k33g...` 10.0.9.x) → **HEALTHY, 0×408**, boot 770s (CPU-surve all).
- ⚠️ Gotcha: Coolify 2 võrku — `db`/`meili` aliasid AINULT `k33g...` (10.0.9.x), MITTE `_default` (10.0.10.x).
  Esimesed 2 katset valel võrgul → `db` ei resolveerunud → pool-timeout (valesti tõlgendatud boot-blokeerijaks).
- Round-robin kinnitatud: `getent medusa` → 2 IP (3:7 jaotus).
- Connection-math: idle 5 pg-conn, 30-concurrent → pg-peak **18** (<30 siht). Host load 9.8/12 vCPU, RAM 9.4/62.

**UUS LEID — pool-starvation üksikinstantsil**
- Koormus 300 req / 30 concurrent `/store/regions` → medusa-1 pool-starvation (unhealthy, health 20s timeout).
- Juur: query.graph relation-expansion (#11922) hoiab pool-conn'e → ammendub. pg server-conn vaid 12 (postgres OK).
- **Ise-taastus ~90s** peale koormust (restart polnud vaja).
- Connection-math RISK: 2 instance × DB_POOL_MAX 50 = 100 = postgres max 100 → **PgBouncer prod'is VAJALIK**.

## Seis lõpus
- Staging: single-instance, meili-fix'iga, **healthy** (medusa-2-test eemaldatud).
- Prod: PUUTUMATA (afcad7e4).
- Doc §14 lisatud (`tasks/2026-06-07-scaling-reliability-architecture.md`).

## PROD-DEPLOY A — meili-fix LIVE ✅ (õhtu, Tarmo prod-go)
- Coolify prod-app `uo28ovobnflauslqjgxeohl0`, main HEAD 42f3d701. Build 173s, boot ~13.5min.
- ✅ 0×408, meili settings 100% terved (searchable 7/filter 13/sort 3/synonyms 7755/rank 6/17441 docs), otsing+filtrid+kategooriad+toode+avaleht+hinnad kõik 200.
- Cutover-cleanup: CF cache'is cutover-akna 404-d → CF purge_everything (node konteineri seest) → re-warm 200. Õppetund: purge→külm-cache 504 esmal, warmib; B-l surgical purge / warm-up.
- 🟡 Pre-existing BUG leitud + juur kinnitatud (MITTE A põhjustatud): **CMS katki prod's.**
  - Juur: `backend/src/modules/cms/db.ts` + `backend/src/api/admin/categorization-queue/route.ts` loovad oma pg-`Client`'i `host: PGHOST||"localhost"`, `port: PGPORT||5435`. Prod-medusas PG* env KÕIK puuduvad → localhost:5435 → ECONNREFUSED ~iga 2.5min (ISR-revalidate CMS-lehed).
  - Vool: storefront `lib/cms.ts` → Medusa `/store/cms/:key` → cms/db.ts → 5435 fail → storefront fallback `lib/cms-fallback/*.json`.
  - Mõju: cms_page tabel on prod-db's (db:5432, **20 rida**: homepage/about/contact/legal-*), AGA backend ei loe neid → live näitab static-fallback'i, **DB-CMS-edits ei jõua live'i; admin CMS-editing katki (loe+kirjuta)**. Sait ise 200 (graceful).
  - **Fix (lihtne, ohutu, vajab prod-go't):** lisa prod-medusa Coolify-env: `PGHOST=db PGPORT=5432 PGUSER=xlmarket PGPASSWORD=<POSTGRES_PASSWORD> PGDATABASE=xlmarket` (osutab samasse DB-sse kus cms_page juba on). Env-lisamine → redeploy/restart (~13min boot). VÕI koodi-fix: parse DATABASE_URL (vajab deploy'd). Severity: VAJA ÄRA TEHA.
- Doc §15 lisatud.

## B3 prep — koodi-fix valideeritud staging'us (Variant 3, commit 6f92f8d5)
- `cms/db.ts` + `categorization-queue/route.ts` → parse `DATABASE_URL` (üks tõeallikas), PG*-fallback ainult lokaalseks dev'iks. EI PG*-env workaround'i (dubleeriks B2-parooli).
- **Staging-valideerimine ✅:** DB-rea muutmine (cms_page about|en title→marker) peegeldus `/store/cms/about` route's → loeb db:5432, MITTE static-fallback. **0×5435, 0×ECONNREFUSED**. DB taastatud.
- Läheb B-deploy'ga prod'i (rebuild niikuinii).

## B2 prep — pg-parool-tarbijate täisnimekiri (rotatsiooniks)
**MUUTA rotatsioonil:**
1. **Postgres auth** (tõeallikas): `ALTER USER xlmarket WITH PASSWORD '<uus>'`.
2. **Coolify-env `POSTGRES_PASSWORD`** (prod-app uo28) → propageerub compose-interpolatsiooniga:
   - `db` konteiner POSTGRES_PASSWORD (init-only, olemasoleval volume'il ei re-apply — aga hoia sünkroonis)
   - `medusa` DATABASE_URL (`postgres://xlmarket:${POSTGRES_PASSWORD}@db:5432/...`)
   - B-future: `medusa-2` + `medusa-worker` (shared env), `pgbouncer` DB_PASSWORD (`${POSTGRES_PASSWORD}`) + userlist.txt kui kasutusel
**AUTO-uueneb (ei eraldi muudatust):** DATABASE_URL (interpoleerub) → medusa ORM, CMS (peale B3), categorization-queue (peale B3), feed-sync-bulk.sh + kõik repo-skriptid (jooksevad medusa sees, pärivad DATABASE_URL).
**EI MÕJUTA (parooli ei kasuta / lokaalne socket-auth):** storefront (medusa-API), redis, meili, backup.sh + coolify-migrate-export.sh + staging-refresh.sh (`docker exec pg_dump` = peer-auth), uptime-kuma (HTTP-checkid). Coolify-managed-DB-backup-e POLE (postgres = compose-teenus).
**Verifitseeri peale:** kõik teenused healthy + pg-auth OK (medusa queryd, CMS, feed-sync, pgbouncer→postgres).

## Järgmine (ootab prod-go't — HARD RULE #1)
- **B (skaleerimine):** PgBouncer + 2 replicat + worker — eraldi öö-aken, eraldi prod-go (kui Tarmo saab jälgida). Connection-math: PgBouncer VAJALIK (2×DB_POOL_MAX 50 = pg max 100).
- Pre-existing ::1:5435 allika jälitus (eraldi, non-fatal).
- Stopgap (12s+retry) JÄÄB kuni load-test prod-arhitektuuril kinnitab.

## B-CUTOVER PROD TEHTUD (2026-06-09 öö, Tarmo jälgis) ✅
- pgbouncer + 2 web-replica't (round-robin 7:5) + worker + parool-rotatsioon (auth 0 kõigis) + B3 CMS-fix (0×5435, cms_page 20 rida) + redis-moodulid. **8h stabiilne**, sait+hinnad OK.
- Load-test: ENNE 36s → PÄRAST 59s (sünteetiline raske untrimmed query.graph; reaal-tee 0s soe = regressioonita; pgbouncer txn-overhead jutukal teel; 2-replica väärtus vajab k6 cart-testi).
- **🔴 PERSISTENTSUS:** worker+medusa-2 MANUAALSED docker-konteinerid (mitte compose's) → **Coolify-redeploy taastaks single-instance'i**. ÄRA redeploy enne fold'imist compose'i (staggered-start). Follow-up + go.
- Homepage külm-render 504 pre-existing (mitte pgbouncer). Stopgap jääb.

### Lõpp-parool-UI-rotatsiooni retsept (assistant-unknown — Coolify API kajas väärtuse)
Kui tahad parooli mida assistant pole näinud:
1. Coolify UI → prod-app uo28 → Environment → POSTGRES_PASSWORD → trüki uus väärtus (assistant ei näe).
2. `docker exec <db-uo28> psql -U xlmarket -d xlmarket -c "ALTER USER xlmarket WITH PASSWORD '<sama uus>'"`
3. Coolify redeploy — AGA esmalt foldi worker+medusa-2 compose'i (muidu kaovad). VÕI: ALTER + restarti pgbouncer+medusa+medusa-2+worker käsitsi uue env'iga.

## Variant D valideeritud staging'us (2026-06-09) ✅
- Otsus: D = pgbouncer + 1 medusa + worker (EI 2. web-replica kuni k6 Sammas 5). pgbouncer jääb.
- **Esimene persistentsuse-katse (medusa-2+worker, sleep 300/600) EBAÕNNESTUS** + degradeeris korraks prodi (jagatud-host overload load 15, homepage 504). Stopp+rollback (vt §18). Prod taastus.
- **Variant D valideeritud** (§19): 2 järjestikust staging-deploy'd, deploy ei timeout, medusa+worker mõlemad healthy, persistentsus OK, max-load 9-11 (host-sõbralik), prod kaitstud warm-keeper'iga (5/5×200 kogu töö vältel).
- **Worker-võrgu-fix (ae4f7bd3):** tühi `networks: default:` → Coolify ei liitnud `_default`'i (xlmarket-redis) → worker crash-loop. Fix: `default: aliases: [xlmarket-worker]`.
- **warm-cache.sh fix:** POSIX-sh (konteineris pole bash) + drop pipefail. Testitud OK.
- Staging on nüüd D-seisus (medusa+worker, peegeldab siht-prodi).
- **Prod-D-redeploy plaan §19** — ootab watched-akna go't. Downtime ~13min dünaamiline (browse CF). Rollback graatsiline. NB: redeploy KUSTUTAB manuaalsed medusa-2/worker (tahtlik, D=single web).

## Variant D PROD-DEPLOY TEHTUD (2026-06-09) ✅
- D-redeploy edukas: pgbouncer + 1 medusa + worker (7 Coolify-teenust, persistentne). Manuaalsed eemaldatud.
- Verifi: pg 11 bounded, CMS 20 rida, auth 0/0, 5435 0/0, /et+kategooriad+toode 200, hind €973.94, worker-mode worker.
- 2 bugi prod-deploy's: (1) warm-cache.sh puudus image's (Dockerfile explicit-COPY) → lisatud + docker cp kohe; (2) warm-cache curl→wget (konteineris pole curl). Scheduled-task */15 enabled.
- PERSISTENTNE: Coolify-redeploy ei kaota worker'it. Homepage külm-504 leevendatud warm-cache'iga. Stopgap jääb. 2. replica + k6 = Sammas 5.
