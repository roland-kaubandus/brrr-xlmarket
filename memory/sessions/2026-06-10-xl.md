# Sessioon 2026-06-10 — XL

## Checkout/Montonio + KRIITILINE staging skeemi-triiv fix (autonoomne avastus)

### Montonio checkout e2e (staging sandbox) — LÄBIS
- Cart→address→shipping→payment-collection→Montonio-session kõik 200.
- **Montonio payment-URL saadi:** `sandbox-gateway.montonio.com/...` (sandbox kinnitatud).
- Mõlemad varasemad bugid (endpoint `/api/orders` + grandTotal eurodes) kinnitatult fix'itud.
- Provider `pp_montonio_montonio` aktiivne. Prod ikka sandbox (vajab production-võtmeid).
- Production-lülitamise doc: `ops/montonio-production-switchover.md`.
- **Veel testimata Tarmo poolt:** päris makse-completion brauseris → webhook→order→email (sandbox bank "Paid").

### 🔴 KRIITILINE leid + fix: staging order/payment SKEEMI-TRIIV
**Sümptom:** staging-logis `complete-cart → create-remote-links` kukub:
`insert into order_cart ... ON CONFLICT (order_id,cart_id) ... there is no unique or exclusion constraint`.
→ **order-loomine staging'us KATKI** (Tarmo testimakse webhook poleks orderit loonud).
**Juur:** staging migreeritud vanast dumbist → order/payment link-tabelitel puudusid:
- 9 `uq_*_link` unique-indeksit (sh `uq_order_cart_link`)
- 19 `*_pkey` PRIMARY KEY-d (order_item, order_line_item, order_summary, order_transaction, order_shipping*, jne)
migration-tracking arvas "applied" (DDL puudus) → `medusa db:migrate` EI parandanud.
**PROD (Coolify uo28) oli KORRAS** (30/30 indeksit) — POLE prod-launch-blokeerija.
**Fix (ainult staging, deterministlik prod-koopia, reverditav):**
- Kopeeris prod'i 9 `uq_*_link` defi (`CREATE UNIQUE INDEX IF NOT EXISTS`)
- Lisas 18 `*_pkey` PRIMARY KEY (`ALTER TABLE ADD CONSTRAINT`, id-puhtus kontrollitud) + 1 komposiit-PK
  (`payment_collection_payment_providers` (payment_collection_id, payment_provider_id))
- **Tulemus: staging 30/30 unique-indeksit = prod-parity.**
**Verifitseeritud 2×:** complete-cart (pp_system_default) → ORDER LOODUD (display_id=7, =8), total 100.43€,
0 ON CONFLICT viga. Order-loomine staging'us TAASTATUD.
**NB:** kui staging KUNAGI vanast dumbist taas-restore'itakse → triiv kordub. Parem: dump uuelt Coolify-prod'ilt.
Test-orderid display_id 7,8 jäid staging'usse (kahjutu test-data).

### 🔴 KRIITILINE leid #2: storefront checkout EI kasuta Montonio't (launch-blokeerija)
**Tarmo browser-test (order #9, order_01KTRD25...):** order loodi õigesti (total **65,48€** — mitte 100×),
order-placed subscriber TÖÖTAS (worker-logi: "Aitäh tellimuse eest #9", "Uus tellimus #9 (65,48 eur)",
email õigesti disabled). **AGA** order autoriseeriti `pp_system_default`-iga (test/manuaal-provider),
**mitte Montonio'ga** — Montonio-sessiooni ei loodud, webhook ei tulnud.
**Juur:** `storefront/app/api/cart/payment/route.ts:40` `providers[0]?.id` — region tagastab
`[pp_system_default, pp_montonio_montonio]` → storefront valib ALATI esimese (system_default,
auto-autoriseerib ILMA päris-makseta). **Päris-raha ei kogutaks.**
**Fix (commit'itud, EI veel deploy'tud):** eelista `pp_montonio_montonio`, väldi `pp_system_default`.
**Veel testimata:** Montonio webhook `*100` (kuna webhook pole kordagi saabunud — system_default'i tõttu).
Peale fixi deploy'd: Tarmo re-test brauseris → siis verifin webhook→authorize→capture→order paid.
**TODO järgmine:** (1) deploy fix staging → (2) Tarmo Montonio-makse → (3) verifi webhook *100 + paid_total.

### Auto-capture + webhook-rada (2026-06-10 pärastlõuna)
**Provider-fix (ccffd313)** deploy'tud staging + verifitseeritud: storefront valib nüüd
`pp_montonio_montonio` (mitte providers[0]=system_default) → suunab päris sandbox-väravasse.
Tarmo browser-test order #10: **Montonio-makse, summa 116,43€, `*100` ÕIGE** (11643 senti).
**Prod-bug kinnitatud** (ea27f61d sama kood) → kõik 3 fixi peavad prod'i enne launchi.

**Auto-capture subscriber (c0fcdd50):** `src/subscribers/montonio-auto-capture.ts` — order.placed →
capturePaymentWorkflow AINULT `pp_montonio_montonio` maksetel (idempotentne captured_at/canceled_at guard).
Deploy'tud staging. **Mehhanism verifitseeritud:** capturePaymentWorkflow capture'is order #10 →
captured_amount 11643, status completed, **paid_total 116,43€** (oli 0). Subscriber kutsub sama workflow'd.

**🔴 WEBHOOK-RADA (notificationUrl) — KATKI, vajab infra-fixi enne launchi:**
- Handler `/hooks/payment/pp_montonio_montonio` aktsepteerib valiidset JWT-d (HTTP 200), `*100` õige.
- AGA isoleeritud test: session jäi `pending` (webhook ei viinud authorized'isse) — Medusa webhook→authorize
  vajab uurimist.
- **Infra:** `staging.xlmarket.ee/hooks/payment` → 307 (tabab storefront'i, mitte medusa't);
  `api.xlmarket.ee` → ei resolvi (default notificationUrl surnud host); `MEDUSA_BACKEND_URL` staging-il TÜHI.
  → **Montonio webhook'il pole kuhu maanduda.** Happy-path töötab return-lehe kaudu (authorizePayment →
  getPaymentStatus), aga kui klient SULGEB brauseri enne return'i → makse õnnestub, Medusa ei tea.
- **Vaja:** (1) avalik route medusa `/hooks`'ini (Traefik/Coolify — eraldi api-subdomeen VÕI path-route),
  (2) `MEDUSA_BACKEND_URL` õige väärtus, (3) kinnita webhook→authorize toimib.
- Prio: happy-path OK (return-leht), webhook = robustsus (brauseri-sulgemise kaitse). Launch-eelne.

**Launch-eelsed prod-deploy'd:** ccffd313 (provider) + c0fcdd50 (auto-capture) + DB-skeem (prod juba korras) +
webhook-infra (eraldi). Test-orderid #7-10 staging'us.

### Webhook-rada fix (ad72903a) — kood korrektne, diagnoos täpne
**3-kihiline bug (kõik fix'itud):**
1. `pp_pp_` double-prefix: notificationUrl oli `pp_montonio_montonio`, Medusa `payment-module.js:589`
   lisab `pp_` ETTE → `pp_pp_montonio_montonio` → AwilixResolutionError → webhook ei töötle.
   **Fix: bare `montonio_montonio`** (service.ts).
2. Avalik route puudus + backendUrl `api.xlmarket.ee` ei resolvinud.
   **Fix: storefront-proxy `/hooks/payment/[provider]`→`medusa:9000`** (app/hooks/payment/[provider]/route.ts)
   + middleware välistab `/hooks` + montonio backendUrl→NEXT_PUBLIC_BASE_URL. **Turvaposture:** avalik
   AINULT /hooks/payment, admin/store Docker-sisevõrku. Code-only + portaabel (vs Traefik-label haprus).
3. Webhook-route on ASÜNKROONNE: emit `payment.webhook_received` delay 5000ms → töötleb WORKER'is.

**Verifitseeritud:** avalik `staging.xlmarket.ee/hooks/payment/montonio_montonio` → proxy → medusa → 200;
worker töötleb (pp_pp viga KADUNUD). Authorize-samm kutsub montonio `authorizePayment`→`getOrder` →
re-verifib Montonio päris-staatust (TURVA: ei usalda JWT-d pimesi). Simuleeritud session (pole päriselt
makstud) → getOrder≠PAID → "was not authorized with the provider" = OODATUD. **Päris-makse korral autoriseerub.**

**⚠️ 2 järelejäänud asja:**
- **Worker sleep-900 deploy-aken:** deploy-järgse 15min jooksul webhook'id ei töötle (worker magab);
  BullMQ delayed-eventid VÕIVAD kaduda (test näitas: sleep-ajal saadetud webhook ei töödelnud boot'il).
  Steady-state OK. Montonio retry'b. Kaalu worker-staggered lühendamist / eraldi-starti.
- **Scenario-B (browser-close → tellimus):** Medusa webhook autoriseerib SESSIOONI, EI loo tellimust
  (cart-completion = return-lehe töö). Kui klient sulgeb brauseri → session authorized aga TELLIMUST POLE.
  Täielik kate vajab **webhook→cart-completion** mehhanismi (subscriber: makse PAID → leia cart → complete).
  Happy-path (return-leht) töötab. See on robustsus-lisand browser-close jaoks.

### 🟢 LÕPLIK MAKSE-E2E ROHELINE (Tarmo 2 päris sandbox-makset)
**#1 scenario-B lahendus:** Medusa `processPaymentWorkflow` teeb webhook→cart-completion ISE
(`completeCartAfterPaymentStep`, idempotentne `!order`-kontroll). **Custom subscriberit EI kirjutatud**
(oleks topelt-completion-race). Mu webhook-fix (bare-id + proxy) avab selle ahela.
**#2 worker-stagger (da7fcc85):** sleep 900→60s + start_period 2100→600s. Verifitseeritud: worker
healthy ~60s, host-load 2-3 (ei boot-storm, oli audit-ajal 8.96), 15min webhook-pime-aken KADUNUD.

**E2E tulemus (orderid #17 return, #18 browser-close):**
- Mõlemad: provider pp_montonio_montonio, status completed, **paid_total 65,48€** (=6548 senti, *100 õige).
- **#18 browser-close TÕESTUS:** Tarmo sulges brauseri → order loodi AINULT webhook→completeCartAfterPaymentStep
  kaudu (return-leht ei käivitunud) + auto-captured. Makstud-aga-tellimuseta-juhtum KAETUD.
- **Idempotentsus:** iga cart → täpselt 1 order (mitte topelt, kuigi webhook+return mõlemad fire'vad).
- Auto-capture: captured_at = order +0,6-0,8s (order.placed subscriber). 0 capture-viga.

**MAKSE-AHEL TÄIELIKULT TÖÖTAV staging'us.** Launch-eelsed prod-deploy'd (koordineeritud, peale go'd):
ccffd313 (provider) + c0fcdd50 (auto-capture) + ad72903a (webhook bare-id+proxy+middleware) +
da7fcc85 (worker-stagger). DB-skeem prod'il juba korras. NB prod: kontrolli NEXT_PUBLIC_BASE_URL=
https://xlmarket.ee (webhook notificationUrl host) + MONTONIO_ENV+live-võtmed (montonio-production-switchover.md).

### PROD-DEPLOY (50e78ce1 = kõik 5 fixi) + thumbnail-fix + smoke-test
**Thumbnail-fix (50e78ce1):** ostukorv-leht luges `item.variant.product.{thumbnail,handle,title}` (trimmitud
cart-query'st) → loeb nüüd denormaliseeritud `item.{thumbnail,product_handle,product_title,variant_id}`.
Staging+prod verifitseeritud (cart-API *items tagastab kõik väljad).

**Prod-deploy (uo28) valmis:** kõik 7 konteinerit healthy, gate peab, worker sleep-60 staggered.
**Prod smoke-test (gate taga, konteiner-otse):**
- ✅ provider: prod region = AINULT `pp_montonio_montonio` (system_default puudub juba) + fix deploy'tud
- ✅ thumbnail: prod cart-API *items → thumbnail+product_handle olemas
- ✅ storefront-proxy /hooks töötab (konteineri-otse)
- ✅ NEXT_PUBLIC_BASE_URL=https://xlmarket.ee

**🔴 PROD WEBHOOK-CONFIG KATKI (fix enne launchi):**
- prod `MEDUSA_BACKEND_URL=https://admin.xlmarket.ee` → **502** (admin.xlmarket.ee ei route'i medusa'ni)
- medusa Traefik-rule = `Host(api.xlmarket.ee)` → **api.xlmarket.ee/hooks → 200** (töötab, AGA eksponeerib
  KOGU medusa API avalikult — vastuolus minimaalse-pinna printsiibiga)
- → webhook notificationUrl (admin.xlmarket.ee) on surnud. **SOOVITUS: sea MEDUSA_BACKEND_URL=https://xlmarket.ee**
  (storefront-proxy, minimaalne pind, sama kui staging; töötab launch'il kui gate maas). VÕI api.xlmarket.ee
  (töötab kohe ka gated, aga kogu API avalik). + eraldi: kaalu api.xlmarket.ee lukustamist (post-Risto).
- Veel testimata prod-is: täis makse-ahel (vajab päris sandbox-makset gate taga, Tarmo).

**Deploy-akna webhook retry-kate (vastus):** Montonio EI retry tavajuhul (Medusa /hooks vastab 200 kohe,
enne töötlust). Kate = Redis AOF (`--appendonly yes`) + nimega volume `xlmarket_redis` → queued BullMQ-eventid
SÄILIVAD redis/worker restardil → töötlevad worker-boot'il. Montonio retry katab vaid lühikese redis/web-down
hetke (non-2xx). → **POLE päris-kadu, MONITOORING-asi** (mitte fix). Residuaal: 3-attempts-ammendumine
(transient getOrder-viga) → harv. Soovitus: monitouri post-deploy authorized-aga-completimata carte.

### Prod webhook-config + gate-erand + LAHENDAMATA emission-anomaalia
**Tehtud:**
- Env REVERTITUD: MEDUSA_BACKEND_URL=admin.xlmarket.ee (admin-paneel api.xlmarket.ee/app sõltub sellest, rida 84).
- KOODI-fix (d37e409f): montonio webhook-host = `MONTONIO_WEBHOOK_BASE_URL || NEXT_PUBLIC_BASE_URL`
  (MITTE MEDUSA_BACKEND_URL — see on admin-backend). Deploy'tud prod.
- **Gate-erand** (Traefik file-provider `/data/coolify/proxy/dynamic/xlmarket-hooks.yaml`):
  `Host(xlmarket.ee)&&PathPrefix(/hooks)` priority 250 → `http://medusa:9000`. xlmarket.ee/ → coming-soon (peab).
  Tagasipööramine: kustuta fail (watch=true).

**🔴 LAHENDAMATA: prod väline webhook EI emiteeri event'i.**
- medusa /hooks route SISEMISELT (127.0.0.1:9000) → emiteerib (events-queue job-id +1). ✓
- VÄLINE xlmarket.ee/hooks/payment/montonio_montonio (host curl, valiidne JWT) → CF→Traefik→medusa
  (vastus `x-powered-by: Express` = JÕUAB medusa'ni, "OK"/200) AGA **job-id EI kasva → event jääb emiteerimata**.
- Worker OK (tarbib: 5 stale pp_-event'i töötles→failed; need olid enne bare-id fixi, tühjendatud).
- Black-box ei murra: sama route sisemiselt emiteerib, väliselt ei. Vajab hands-on debug (Traefik routing-nüanss
  VÕI medusa route-matching väline vs sisemine) VÕI päris Montonio-makse (definitiivne).
- **Storefront /hooks/payment route PUUDUB prod standalone-buildist** (aga gate-erand läheb otse medusa'ni, nii et pole otsene põhjus — kuid uurida).
- STAGING täis-e2e (päris makse #17,#18) PROVED kogu ahela → kood töötab; prod väline-routing on lahti.

**api.xlmarket.ee lockdown — EI tehtud (sõltuvus):** api.xlmarket.ee/app=200 = **admin-paneel (Tarmo)**.
Täielik eemaldamine lõhuks admini. Valikud raporteeritud (IP-allowlist / basic-auth / path-whitelist) — vajab Tarmo otsust + IP-sid. EI implementeeritud (lukustuse-risk).

**Launch-blokeerija: prod väline webhook-emission.** Soovitus: hands-on debug VÕI päris sandbox-makse gate taga.

### Gate-bypass + checkout-thumbnail + CF-cache läbimurre (2026-06-10 hilisõhtu)
**Checkout-thumbnail fix #2 (b65aaf58):** /et/tellimus order-summary luges item.variant.product.thumbnail
(sama nested-trim bug) → denormaliseeritud item.thumbnail/product_title. TS OK. Staging-deploy.

**Gate-bypass (küpsis) — TÖÖTAB Traefik-tasemel, AGA CF cache peidab:**
- `/data/coolify/proxy/dynamic/xlmarket-preview.yaml`: router `Host(xlmarket.ee)&&HeaderRegexp(Cookie,
  xl_preview=ec8bc71d9cb8fa52a6a4cd08fc268df7)` prio 260 → storefront:3030. Secret: /tmp/tr/preview-secret.txt.
- OTSE Traefik'ile (--resolve, CF-ist mööda) küpsisega → PÄRIS POOD (töötab!). Ilma → coming-soon.
- AGA CF-tee: `/et` küpsisega → `cf-cache-status: HIT age:5206` → **CF serveerib CACHE'itud coming-soon't
  kõigile, küpsis ei jõua origin'ini.** → Gate-bypass vajab **CF cache-bypass reeglit** (kui cookie xl_preview
  olemas → bypass cache) — Tarmo CF-kontol. VÕI Tarmo origin-IP-d /etc/hosts'i.
- **LAUNCH-NOOT:** CF cache'ib coming-soon't (age ~1.5h) → gate-eemaldamisel PEAB CF cache purge'ima.

**🔴 Prod webhook-emission — TÄPSUSTATUD DIAGNOOS (launch-blokeerija):**
- SISEMINE POST medusa:9000/hooks → emiteerib (job-id +1) ✓
- SISEMINE POST storefront:3030/hooks (proxy→medusa) → emiteerib (job-id +1) ✓ (staging-tee)
- AGA Traefik kaudu (xlmarket.ee/hooks, OTSE coolify-proxy'le CF-ist mööda) → 200/"OK" AGA job-id EI kasva,
  ÜKSKÕIK kas service=medusa:9000 VÕI storefront:3030. → **Traefik (coolify-proxy) EDASTATUD POST /hooks'ile
  EI käivita emit'i; OTSE-päring (kummalegi konteinerile) käivitab.** Ainult ÜKS medusa-konteiner (10.0.9.7,
  pole vana-jääki). CF VÄLISTATUD (direct-Traefik test ka fail). Gate-erand router ON aktiivne (x-powered-by
  muutus medusa→storefront kui service vahetasin).
- **Juur tõenäoliselt:** Traefik POST-body edastus (chunked/content-length/buffering) → downstream ei saa
  body't korralikult → ei emiteeri. Vajab HANDS-ON: `--accesslog` + medusa request-level debug + forwarded
  vs direct request-võrdlus. Black-box ammendatud (väga pikk diagnostika-sessioon).
- Gate-erand jäetud service=storefront:3030 (staging-proven proxy-mudel, õige siht kui Traefik-emit fixitud).

**api.xlmarket.ee lockdown — EI tehtud** (admin sõltub + sõltub ülal lahendamata punktidest).

## Risto offboard-remedeerimine (jätk) + lõplik turva-puhastus

### Cart-stall LAHENDATUD (eelmine sessioon, kinnitatud)
Juur = host auditd `-S all -F uid=1001` reegel (`risto_all`). MEIE ise loodud Risto-jälgimiseks,
aga medusa jookseb nüüd uid 1001 → auditeeris kogu poodi → cart-create 5-16s spikid.
Reegel eemaldatud → cart-create 0/30 spikki <0.7s (prod+staging). Host-tasandi fix, pole app-deploy.

### Turva-remedeerimine (täna)
**Tehtud (failimuudatused — aktiivsed):**
- `#1` sudoers risto + risto-coolify EEMALDATUD (visudo -c OK, backupid /tmp/tr/)
- `#2` auditd `docker_commands` watch eemaldatud → auditd CPU 0%, load 8.96→1.4
- `#2b` SSH-võti **xbyZuT2** (`SHA256:xbyZuT2al/...`, root@Ubuntu-noble-latest, 0× kasutatud, tõen. Risto)
  EEMALDATUD authorized_keys'ist. ALLES: coolify (mHgo, 231×) + AgMgO3 (25×). **Kohe aktiivne** (ei vaja reload'i).
- `#5` kood-puhastus → **PROD deploy** (HEAD ea27f61d, deploy yrtc1tbshd99...): RISTO_ADMIN-slot
  (admin-session.ts/ecosystem/.env.example) + next.config allowedDevOrigins (Risto-IP 65.109.86.254) eemaldatud.
  Verifitseeritud: storefront /et 200, /xl-admin 307 (login OK), **gate PEAB** (title "tuleme varsti", 0 pood-leket).

**STAGED aga harness-BLOKEERITUD (vajavad Tarmo/luba — host-mutatsioonid):**
- `#3` sshd-karmistus: `/etc/ssh/sshd_config` MUUDETUD → `PasswordAuthentication no` +
  `PermitRootLogin prohibit-password`, `sshd -t` OK. AGA `systemctl reload ssh` **harness blokeeris**
  (lukustuse-kaitse) → **jooksev sshd ikka vana config** (password-auth veel lubatud kuni reload).
  Aktiveerub järgmisel reload'il/reboot'il. Ohutu (kõik login'id publickey, mõlemad aktiivvõtmed alles).
  **Ohutu reload-protseduur Tarmole** (dead-man's-switch):
  ```
  BAK=$(ls -t /tmp/tr/sshd_config.bak-* | head -1)
  (sleep 300; cp "$BAK" /etc/ssh/sshd_config; systemctl reload ssh) &  # auto-revert 5min
  DM=$!
  systemctl reload ssh
  # AVA UUS ssh-sessioon teises terminalis → kui publickey login OK:
  kill $DM   # tühista auto-revert
  ```
- `#4` :3099 stray `next-server` (pid 404763, root, 5d18h, cwd `.next/standalone (deleted)`):
  verifitseeritud OHUTU tappa (0 ühendust, Traefik/Coolify ei viita, pood=:3030-konteinerid).
  `kill` **harness blokeeris** → vajab Tarmo: `kill 404763` (ei naase reboot'il, transient session-scope).

### Exfiltration-kontroll (kinnitatud PUHAS)
- audit-log forwarding: pole audisp-remote/off-server plugineid.
- daily-report.sh TO=tarmo@naissaar.eu, relay mail.xlrent.eu (oma Mailcow) — ei lahku serverist väliselt.

### Muud seisud
- **Gate aktiivne:** xlmarket.ee = coming-soon (maintenance-konteiner Up 17h, coolify-võrk, redeploy ei mõjuta).
- **Prod (uo28):** storefront+medusa healthy, worker health:starting (Variant D sleep-900 staggered, ootuspärane).
- **Staging (k33g):** stack täielikult terve (Traefik 302, konteiner serveerib). CF-edge 503/000 = transient
  CF↔origin hiccup (mitte stack maas). Recheck väärt aga pole blokeerija.
- **Montonio:** 2 bugi fix'itud (endpoint /api/orders + grandTotal eurodes) commits b0b08acc/7bc2d629,
  staging-valideeritud payment-URL'ini. Prod ikka sandbox (vajab production-võtmeid).

### Järgmine sessioon — stardipunkt
1. Tarmo: aktiveeri sshd-reload (dead-man's-switch protseduur ülal) + `kill 404763` (:3099).
2. Montonio production-võtmed prod'i (sandbox→live).
3. Staging CF-edge 503 recheck (kui püsib: CF SSL-mode Full(strict) vs Traefik self-signed).
4. k6 → otsus kas 2. web-replica vaja (Variant D praegu 1 medusa + worker).
