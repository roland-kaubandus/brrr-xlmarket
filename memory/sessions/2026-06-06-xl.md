# Sessioon 2026-06-06 — launch-blokeerijad, KRIITILINE cart-debug, tõlked

> Live: xlmarket.ee (Coolify, 65.21.126.235). App uuid `uo28ovobnflauslqjgxeohl0`.

## Tänased commitid (kronoloogiliselt)
- `e9f63531` Montonio backend fix: jsonwebtoken + @types (build kukkus TS7016)
- `8a5778bd` Session log: öine redeploy + maintenance-gotcha
- `89879a64` Launch-audit + PUNCH-LIST sync
- `e7cc0f5b` B2: legal-lehed ET (5) + locale-aware fallback + .eu→.ee
- `06701e8b` UI ET-tõlge 1. pass ~200 stringi (ostukorv/checkout/tooteleht/filtrid/footer/konto)
- `36a4b8b2` salon-spa püsifix (generateStaticParams L1+L2) + CmsMarkdown tabel overflow-x
- `c1c88bb6` Feed-sync FAAS 1+2 (bulk-price + sync-synonyms + orkestraator + Dockerfile)
- `5e51e6ec` KRIITILINE: cart/salon-spa fix (autoSelectFamily) + Montonio hostname + breadcrumb ET
- `a51849bc` Stopgap: cart timeout 3s→12s + retry
- `d1f3f033` Stopgap tune: cart 2×13s
- `bbc652f9` Session log
- `49a7092c` Kategooria-leitavus fix (32 toodet → L1) + handoff
- `a14569c6` UI ET-tõlge 2. pass (avaleht/SearchBar/login/register/ärikliendile/alustajale)

## 🔴 CART-STALL ROOT CAUSE LEITUD (staging-profiling, 2026-06-06 õhtu)

**Reprodutseerus staging'us** (medusa-k33g510dw19uyjau3ca7dqpi-071750594209): cart-POST 0.1s / 4.6s / 9.9s / 15s intermittent — sama muster kui prod.

**Profiling-tulemused (strace -f -T + strace -c idle vs load + per-thread CPU + cgroup):**

| Test | Tulemus | Järeldus |
|---|---|---|
| **Syscall-klassifikatsioon** | 100% stall = `futex FUTEX_WAIT_PRIVATE` (condvar). Main-thread (2266587) pargib `futex(0x..fd9fb8, NULL)` ~4.5s, libuv-worker `FUTEX_WAKE` äratab | **In-process condvar-ootus** (mitte timer/sleep/socket) |
| **idle vs load** | futex 10 kõnet (idle) → **3276 (load)**, aeg 0.009s→1.166s (89.85%) | futex skaleerub cart'idega → cart-driven |
| **GC?** | MEM **flat 159 MiB** (ei kasva) | GC VÄLISTATUD |
| **CPU-bound?** | burst CPU **24% / 12 tuumast**; strace-c ainult 1.166s syscall-aega 14s aknas → enamik stall'ist **userspace CPU tausta-threadil** | Off-syscall userspace töö |
| **CPU-throttle?** | `cpu.max: max`, `nr_throttled 0`, 0 resource-limiite | THROTTLE VÄLISTATUD |
| **per-thread CPU** | **4× libuv-worker** (PID 2266589-92, UV_THREADPOOL_SIZE=4) põlevad 10-17 jiffied/burst | **libuv threadpool = pudelikael** |
| **DNS?** | 8 cart'i = 2 UDP:53 lookupi; backendid keep-alive | DNS VÄLISTATUD |
| **redis distributed-lock?** | locking=**in-memory**, CACHE/EVENTS/WE_REDIS_URL **tühjad**, redis-vigu 5/60s (pole storm) | redis-lock VÄLISTATUD |
| **KONTENTION** | isoleeritud cart (7s vahe, 0 kattuvust) **STALLIB ~30%** (intrinsic); **6 paralleelset → KÕIK >16s**, 20 paralleelset → kõik stall | Threadpool-of-4 serialiseerib → kontention võimendab katastroofiliselt |

**ROOT CAUSE:** Medusa 2.x cart-create dispatch'ib **CPU-blokeerivat tööd (crypto/fs — DNS välistatud)** vaikimisi **4-threadi libuv threadpool'i**. Main-thread (event-loop) pargib condvaril (futex) oodates pool'i tulemust. 4 threadi + N samaaegset cart'i → pool ammendub → kõik cart'id ootavad ~5s. Tühi cart = absurdne, sest aeg ei kulu DB-tööle vaid threadpool-järjekorda.

### UV_THREADPOOL_SIZE=64 TEST — ❌ EI AIDANUD (2026-06-06 hilisõhtu, staging)

Testitud empiiriliselt: lisatud `UV_THREADPOOL_SIZE=64` Coolify env'i (staging app `k33g510dw19uyjau3ca7dqpi`), restart, boot. Konteineris 71 threadi (64 libuv-workerit jõustusid). **Tulemus identne enne-seisuga:**
- Isoleeritud cart (7s vahe): 4.7/0.1/0.1/4.9/0.1/9.7 + warm-up 8.9/4.5/4.9/4.3 → **ikka stallib ~50%**
- 6 paralleelset: **KÕIK 16s timeout** (identne, 0 paranemist)
- CPU burst: **19.9%** (madal, nagu enne 24%); MEM flat 183MB
- strace UV=64 ajal: **futex ikka 82%** (3801 kõnet, 865 EAGAIN-viga)

**JÄRELDUS:** threadpool-exhaustion (4 threadi) ÜMBER LÜKATUD + CPU-bound välistatud. Pudelikael = **ÜKS in-process serialiseerimispunkt** (lukk/condvar, EAGAIN-contention), MITTE pool-laius. 64 threadi ei aita sest kõik ootavad sama luku taga.

### TEGELIK JUUR: Medusa in-memory workflow-engine + locking (dev-only)
medusa-config'is **EI ole** workflow_engine/locking moodulit defineeritud → mõlemad default = **in-memory** (Medusa dokumentatsioon: inmemory on AINULT arenduseks, serialiseerib workflow'd in-process). Cart-create käib workflow-engine'i kaudu → in-process lukk/orkestratsioon serialiseerib → futex-contention, ~5s, paralleelne katastroof.

**Kõik 4 redis-providerit ON juba installitud** konteineris: `workflow-engine-redis`, `locking-redis`, `event-bus-redis`, `cache-redis`. Vaja AINULT konfigureerida + töötav redis.

**JÄRGMINE SAMM (vajab user-kinnitust — muudab medusa-config.ts + rebuild + redis):**
1. **Konfigureeri redis-moodulid** medusa-config'is (workflow-engine + locking + event-bus + cache → redis). See on Medusa **prod-standard**.
2. **Paranda staging redis** — praegu `REDIS_URL=redis://redis:6379` → mailcow redis (NOAUTH/EAI_AGAIN). Vaja staging oma redis-teenust (redis-k33g) või töötav auth. PROD-l redis töötab (xlmarket-redis) → prod saab kohe redis-moodulid.
3. Re-test sama protokolliga (isoleeritud + paralleelne + CPU).

UV_THREADPOOL_SIZE=64 jäi staging env'i (kahjutu, aga mõjuta). Stopgap (12s+retry) jääb kuni redis-moodul fix.

### 🔴 SUUR PÖÖRE: POLE cart-spetsiifiline — GLOBAALNE event-loop freeze (2026-06-07 öö)

Testisin staging'us redis-moodulid + worker-mode. **Mõlemad EI aidanud.** Süvendatud profiling paljastas, et probleem on hoopis laiem kui arvasime:

**Empiiriliselt VÄLISTATUD config-hoovad (kõik testitud staging'us, kõik EI aidanud):**
| Test | Tulemus |
|---|---|
| UV_THREADPOOL_SIZE=64 | 6 paralleelset → kõik 16s (muutumatu) |
| Redis workflow-engine+locking+cache+event-bus (kõik 4 ühendatud, kinnitatud) | 6 paralleelset → kõik 16s (muutumatu) |
| MEDUSA_WORKER_MODE=server (taustatöö HTTP-protsessist välja) | 6 paralleelset → kõik 16s (muutumatu) |

**KRIITILINE AVASTUS — pole cart-spetsiifiline:**
- 8 paralleelset **GET /products** → KÕIK 16s (read'id ka katki!)
- **/health** (lihtsaim endpoint, ilma auth/DB) paralleelse koormuse all → **6.6s**
- /health IDLE (koormuseta) = **1ms** → freeze on PUHTALT koormus-triggeritud
- Sekventsiaalne /products = enamasti 0.17s aga juhuslik ~5s spike

**Mõõtmised freeze'i ajal (8-10 paralleelset päringut):**
- medusa CPU = **1.5%** (protsess IDLE — MITTE GC, MITTE arvutus)
- DB = idle (2 ühendust, 0 blokeeritud lukku, 8/100 conn) — DB EI tee midagi
- mälu flat (180-210MB)
- futex 90% (3800 kõnet, 865 EAGAIN = lock-contention/convoy), 17% sys-CPU
- ükski I/O-syscall EI hangu >0.5s (pole aeglane connect/poll/recvfrom/DNS)
- main-thread pargib futexil (FUTEX_WAIT_PRIVATE, NULL-timeout), worker-thread äratab

**JÄRELDUS:** Medusa event-loop **KÜLMUB perioodiliselt ~5-16s koormuse all**, OFF-CPU (puhas blokeeruv ootus, mitte arvutus). Kõik endpoint'id mõjutatud. **Pole lahendatav config-tasemel** (threadpool/redis/worker-mode kõik läbi proovitud). Juur on **Node v22 runtime / libuv / Medusa framework concurrency-tasemel** — vajab JS-async profiling'ut (clinic.js bubbleprof / node --prof / diagnostic-report) täpse blokeeriva koodirea leidmiseks. Eraldi fokuseeritud sessioon.

**VÄLISTATUD ka:** GC (CPU idle), native-addon'id (ainult msgpackr), telemetry (puudub), autoSelectFamily (--no-network-family-autoselection ON), DNS (kiire), slow-connection (ükski connect ei hangu), DB-lukud (0).

**Staging praegune seis:** UV=64 + redis-moodulid (config+env, dedikeeritud redis 10.0.9.8 container `xlmarket-redis-staging`) + MEDUSA_WORKER_MODE=server. medusa-config.ts redis-tugi **commit'itud main'i (fa857912, d6bfcacc)** — OHUTU prod'ile (env-driven, prod jääb in-memory). Stopgap (12s+retry) jääb prod'is.

### SAMM 1+3 tulemused (logger-diferentsiaal + GitHub) — 2026-06-07

**SAMM 1 — LOG_LEVEL=error (logimine peaaegu välja):**
`@medusajs/cli reporter` kasutab **winston Console → stdout**, default `LOG_LEVEL=http` → IGA päring logitakse. Seadsin staging `LOG_LEVEL=error` (0 http-logi kinnitatud).
- 8 paralleelset /products: **16s (timeout) → 6.5-7.7s** → logimine lisab ~7-9s backpressure'i (winston sync-write stdout Docker-pipe'i, koormuse all täitub).
- AGA freeze EI kadunud → logger pole AINUS süüdlane.

**Puhas concurrency-skaala (LOG_LEVEL=error, /products):**
| Test | Ajad | Muster |
|---|---|---|
| Sekvents 5× | 0.18/0.18/0.17/**9.3**/0.18 | üksik päring juhuslik ~9s freeze (~20%) |
| 2 paralleelset | 4.7/4.7 | mõlemad KOOS |
| 4 paralleelset | ~5.0 (kõik) | kõik KOOS |
| 8 paralleelset | 6.5-7.7 (kõik) | kõik KOOS |

**Tõlgendus:** kõik lõpetavad KOOS (mitte staircase) → pole klassikaline serialiseerimine, vaid **korduv ~5-9s event-loop FREEZE**. Päringud, mis satuvad freeze-aknasse, ootavad koos ja vabanevad korraga. Üksik päring tabab freeze'i ~20%. Logimine lisab eraldi backpressure-kihi peale.

**SAMM 3 — GitHub:** Medusa **2.13.5**. Leitud teadaolevad issue'd:
- **#11922 "Requests being executed serially"** — pure `setTimeout` route serialiseerub (2.x framework-level, OPEN, no fix). Kattub meie mustriga.
- #6318 server-not-responding-under-load (event-loop 1K events/iter overwhelm).
- #5658 server stalls under high load.
→ **Teadaolev Medusa 2.x framework concurrency-piir.**

**SAMM 2 (definitiivne off-CPU stack) — BLOKEERITUD:** gdb/lldb/llnode PUUDUVAD konteineris+hostis, `apt-get install` denied (sandbox). gcore vajab gdb-d. Alternatiiv: `NODE_OPTIONS=--report-on-signal` (Node built-in, install-vaba) + SIGUSR2 freeze'i ajal → JS+native stack. VÕI `blocked-at` npm (vajab installi). **Vajab user-otsust.**

**Healthcheck-restart risk:** koormus-test tekitas medusa restarti (freeze → /health timeout → Coolify healthcheck kill). VÕIB prod'is samuti juhtuda koormuse all → restart-loop = downtime. (+ meili plugin rakendab indexSettings igal boot'il → juhuslik 408-timeout boot'il, lisab ebastabiilsust.)

### PRAKTILINE LAUNCH-SOOVITUS (kuni root-cause)
1. **LOG_LEVEL=error (või warn) → PROD** — mõõdetud ~2-3× paranemine koormuse all (16s→6.5s). Ohutu, pööratav env.
2. **Horisontaalne skaleerimine** — mitu medusa `server`-replica't Traefiku taga. Iga protsess freeze'ib eraldi → N replica't = N× concurrency-talumine. EI lahenda üksiku päringu ~9s freeze't.
3. Root-cause (~5-9s base-freeze) vajab sammu 2 (off-CPU stack) VÕI Medusa versiooni-vahetust.

### 🎯 SAMM 2B blocked-at — DEFINITIIVNE JUUR (2026-06-07)

Lisatud `blocked-at` (npm) + preload `debug-blocked-at.cjs` (gate DEBUG_BLOCKED_AT, prod-ohutu), NODE_OPTIONS=--require Dockerfile RUNNER-ENV'i (PEALE npm ci+COPY — muidu build-aegne npm ci kukub "Cannot find module"). Commits 06eba866, 16784866. blocked-at async_hooks-overhead aeglustab boot'i drastiliselt (~33min) + võimendab ajad 2-3×, AGA blokeeriva koodi ASUKOHT on usaldusväärne.

**Request-aja blokeerivad stack'id (koormuse all):**
| Blokeeriv callback | ms |
|---|---|
| `pg Client._connect → net Socket.connect` (uue pg-ühenduse loomine) | 13846 (TCPWRAP) |
| `knex Runner.ensureConnection` (pool-ühenduse hankimine) | 10019 / 4397 / 4572 |
| `ensurePublishableApiKeyMiddleware` (iga store-päring → DB-päring) | 9729 / 2340 |
| `ProductRepository.findAndCount` (mikro-orm find) | 5213 |

**ROOT CAUSE:** Medusa MikroORM/knex **DB connection-pool acquisition** on pudelikael. `node_modules/@medusajs/utils/.../mikro-orm-create-connection.js:99` → `pool: { min: 2, ...database.pool }`, max seadmata → **knex/tarn default max = 10**. Koormuse all: iga store-päring teeb pubkey-middleware DB-päringu + sisupäringu → pool ammendub / uute ühenduste loomine aeglane → päringud blokeeruvad `ensureConnection`-is (off-CPU) → **seletab varasema "DB idle" paradoksi** (päringud ootavad pool-ühendust APP'is, DB näeb idle-ühendusi).

**KAAVIAT:** blocked-at overhead on promise-rohkel DB-koodil kõige suurem → võib stack'e DB poole kallutada. Otsustav kinnitus = pool-suurenduse test ILMA blocked-at'ita.

**JÄRGMINE SAMM:** 1) eemalda blocked-at overhead (DEBUG_BLOCKED_AT env maha). 2) Tõsta pool medusa-config'is (`projectConfig.databaseDriverOptions`/pool → min:10, max:50+). 3) Re-test concurrency PUHTALT. Kui freeze kaob → pool kinnitatud. 4) Fix prod'ile (pool-config ohutu, alati kasulik).

**Side-fix vaja:** dedikeeritud staging redis `xlmarket-redis-staging` loodud `--maxmemory-policy allkeys-lru` → BullMQ/workflow-engine hoiatab "should be noeviction" → muuda noeviction.

### 🎯🎯 JUUR TÄPSELT LOKALISEERITUD (2026-06-07) — query.graph relatsiooni-laiendus

Pool-fix (DB_POOL min:10/max:50, commit 27a3353e) test:
- **4 paralleelset /products: 5s→0.2s ✅** (pool aitas madalal concurrency'l)
- **8+ paralleelset: ikka 13-20s freeze** → pool POLE täisjuur.

**DB pool VÄLISTATUD täisjuurena:** freeze'i ajal pg = **10 idle + 1 active** (max_conn 100) — ühendused VABAD, päringud serialiseeruvad (1 active korraga). blocked-at oli overhead-kallutatud DB-koodile.

**Lokaliseerimine concurrency-testidega (pool 50, blocked-at off):**
| Endpoint (8 paralleelset) | Tulemus |
|---|---|
| /health | KÕIK instant (0.001-0.006s) → HTTP/event-loop terve |
| /store/regions (kerge query.graph) | KÕIK kiired (0.02-0.05s) |
| /store/products (default fields) | **13.9s KÕIK** (freeze) |
| /store/products?fields=id,title (minimaalne) | 0.04/0.05/0.06 + 4.5×5 (dramaatiliselt parem) |
| sega 4×health+4×products | products lohistab health'i 3.5s'ni (üks event-loop) |

**ROOT CAUSE:** Medusa **`query.graph` (RemoteJoiner) SÜGAV relatsiooni-laiendus** rasketel endpoint'idel (/store/products default = variandid→hinnad→optionid→pildid→kategooriad, palju sub-päringuid; cart sama). Üksik raske päring kiire (0.18s), AGA **8 paralleelset → super-lineaarne plahvatus** (13.9s) — sub-päringud serialiseeruvad/ujutavad event-loop'i (off-CPU, DB idle). Kerged päringud (/health, /regions, minimaalsed fieldid) immuunsed. = Medusa 2.x #11922 muster, AGA spetsiifiline raske-relatsiooni-päringule.

**FIX-SUUNAD (prioriteedis):**
1. **Vähenda relatsiooni-laiendust hot-päringutel** — storefront toote-detail (`/api/product/[handle]` → getProduct) + cart: küsi ainult vajalikke fielde. SUURIM võit.
2. DB pool (tehtud, env-driven) — aitab madalat concurrency't.
3. Horisontaalne skaleerimine (N replica't = N× concurrency).
4. LOG_LEVEL=error (vähendab võimendust).
NB: toote-SIRVIMINE = MeiliSearch (ei mõjuta). Mõjutatud = cart + toote-detail (query.graph).

### FIX #1 — field-trim (2026-06-07, pooleli)

**Bisektsioon (8-paralleelne, üksik relatsioon, getProduct'i päris-handle):**
| Relatsioon | max-aeg |
|---|---|
| `*categories` | **15.8s** (suurim tapja — parent-ahel laiendus) |
| `*variants.calculated_price` | 9.9s (pricing-moodul, UI vajab hinda) |
| metadata | 7.8s (tõen. müra — JSONB column, mitte relatsioon) |
| variants (spetsiif) | 5.0s |
| inventory_quantity | 4.8s |
| `+images` | 0.12s (odav) |

**getProduct trimmitud** (`storefront/lib/medusa.ts:187`): wildcardid (`*variants`, `*options`, `+categories`) → spetsiifilised fieldid. `+categories` → `categories.id,name,handle` (eemaldab 15.8s parent-ahela). **UI-andmed kontrollitud terved** (categories name+handle, variant calc_price=83259, options, metadata _et, 10 pilti). EI ole veel deploy'tud (storefront rebuild vaja).

**TENSIOON:** trim hoiab `calculated_price` (UI vajab hinda) mis on ise raske (9.9s 8-paralleelselt) → trim ÜKSI ei pruugi freeze't täielikult kaotada. **Otsustuspunkt:** toote-detail võiks hinna võtta **MeiliSearch'ist** (juba kasutab Meili't title/desc/images jaoks; Meili hind eelarvutatud) → drop calculated_price Medusa-päringust → kerge päring. Risk: Meili-hind vs Medusa-hind sünk.

**Staging degradeerunud** kümnetest concurrent-testidest → puhas A/B vajab medusa-restarti. **Järgmine:** restart → puhas test (current vs trim vs trim-ilma-calc_price) → otsus calc_price/Meili kohta → storefront deploy + UI visuaal-verify. Cart-endpoint sama ravi (Medusa-internal, raskem).

### FIX #1 testid — field-trim AITAB osaliselt, EI lahenda täielikult (2026-06-07)

**Otsus A rakendatud (commit fa59a298 + lokaalsed editid):**
- `getProduct` (lib/medusa.ts) → minimaalne: drop calc_price/inventory/options/`*`-wildcardid; keep metadata+images+minimal-categories+variant.id
- route (`/api/product/[handle]`) → hind **Meili'st** (meiliHit.price, Medusa fallback)
- Põhjendus: KÕIK 17105 toodet = 1 variant, Meili price (832.59) === Medusa calc_price (83259¢). UI-andmed verifitseeritud terved.

**Puhas A/B (restart iga testi vahel degradatsiooni tõttu):**
- Üksik päring: alati kiire (0.18s)
- variants-only (`id,title,variants.id`) 8-paralleelne PUHAS: **0.06s (kiire!)** → minimaalne cross-module link EI ole probleem
- MIN (metadata+images+categories+variants) 8-paralleelne PUHAS: **9.8-13s (freeze)**
- `*categories` 15.8s, calc_price 9.9s (bisektsioon) = tegelikud tapjad
- **Degradatsioon EI taastu kohe** — üks 8-burst jätab serveri aeglaseks (~22s) järgmisteks testideks → mõõtmine kontamineerub

**AUS JÄRELDUS:** Field-trim VÄHENDAB koormust (õige + ohutu, hoia) AGA EI kaota concurrency-freeze't täielikult — raske query.graph relatsiooni-laiendus (RemoteJoiner) koormuse all on **Medusa 2.x framework-piir (#11922)**, mitte ühe field'i probleem. Black-box A/B jõudis piirini (test-kontaminatsioon).

**PRAKTILINE LAHENDUS (prioriteedis):**
1. **Horisontaalne skaleerimine** — N medusa `server`-replica't Traefiku taga = N× concurrency enne freeze't. PÕHILAHENDUS per-protsess concurrency-piirile.
2. Field-trim + Meili-hind (tehtud, ohutu, vähendab koormust) — deploy storefront.
3. DB pool 10/50 + LOG_LEVEL=error (tehtud, marginaalne abi).
4. Healthcheck-timeout tõsta (väldib restart-loop'i koormuse all).
5. Cart (POST /store/carts) = Medusa-internal RemoteJoiner → ainult skaleerimine + framework-fix aitab.

**Staging seis:** mitu docker-restarti tehtud; medusa healthy. Storefront-editid (getProduct trim + Meili-hind) lokaalselt commit'itud, EI veel deploy'tud (vajab storefront rebuild + UI-verify).

### ✅ FIX #1 DEPLOYED + UI-VERIFITSEERITUD (2026-06-07)

Storefront deploy (commit 0aa89747): getProduct minimaalne field-set + toote-detail hind Meili'st. **UI-verify staging (port 3030) — KÕIK terve:**
- `product.variants`: 1 variant + id `variant_01KNXX...` (add-to-cart töötab), sku ✅
- priceFormatted **€832.59** (Meili'st, === Medusa täpne) ✅
- richDescription 5480, breadcrumb ET (Home→Käsi-ja elektritööriistad→Elektritööriistad→Lihvijad), localizedTitle ET, 10 pilti, 5 selling-point ✅
→ Field-trim + Meili-hind **töötab tootmiskõlblikult**, UI ei katke. Vähendab toote-detail query.graph koormust (cache-miss'idel).

### SKALEERIMISE-TEST — connection-math KINNITATUD siduvaks piiranguks

Spin up 2 medusa-replica't (sama image, pool max 25) round-robin testiks. **MÕLEMAD CRASHISID boot'il:**
> `Loaders for module Tax failed: Knex: Timeout acquiring a connection. The pool is probably full.`

**KINNITAB connection-math hoiatuse:** Coolify-medusa (pool max 50) + 2 replica't (25+25) → boot'il **pg max_connections (100) ületatud** → replica'd ei saa ühendust → crash. Replica'd eemaldatud, staging taastatud (pg 4/100, /health 200).

**JÄRELDUS skaleerimisele:** horisontaalne skaleerimine NÕUAB connection-pooling lahendust:
1. **PgBouncer** (transaction-pooling) DB ette — N medusa-instantsi → 1 pg-conn-pool. PARIM lahendus.
2. VÕI tõsta pg `max_connections` 100→300 + igale instantsile pool max ~25-30 (N×pool < max_conn).
3. Coolify natiivse skaleerimise (replicas) + Traefik-LB seadistus.
4. Healthcheck-tolerants (pikem timeout/start-period/retry) — koormuse all aeglane instants ei tohi kill'ituda → restart-kaskaad. LB suunab aeglasest eemale.

Round-robin concurrency-test (8/16/24 üle N replica) JÄÄB tegemata — vajab esmalt PgBouncer/max_conn fix'i (muidu replica'd ei boot'i). Skaleerimise PÕHIMÕTE (N× concurrency-lävi) on aritmeetiline; siduv piirang = pg-ühendused (nüüd kinnitatud).

### 🎯 CART-STALL — TÄISKOKKUVÕTE (uuring lõpetatud 2026-06-07)
**Juur:** Medusa 2.13.5 `query.graph`/RemoteJoiner SÜGAV relatsiooni-laiendus koormuse all → off-CPU event-loop freeze (8+ paralleelset → 5-22s; üksik 0.18s). DB+CPU idle freeze ajal. Framework-piir (#11922).
**Välistatud (empiiriliselt):** UV_THREADPOOL=64, redis-moodulid, worker-mode, DB-pool (10 idle conn freeze ajal), GC, throttle, DNS, logger (ainult võimendab ~7s).
**Tehtud (prod-ohutud, env-driven main'is):** getProduct field-trim + Meili-hind (deployed staging), DB_POOL env (10/50), LOG_LEVEL=error, redis-moodulid, blocked-at-debug (gate'itud).
**Launch-mitigatsioon:** stopgap (12s+retry) prod'is JÄÄB; field-trim+pool+log env'idega prod'i; **horisontaalne skaleerimine + PgBouncer = tegelik lahendus** (prod-infra otsus).

### ✅ SKALEERIMISE POC — PgBouncer + replica't (2026-06-07, staging)

Täisarhitektuur: `tasks/2026-06-07-scaling-reliability-architecture.md` §12 (commit 93c4c450).

**Tõestatud staging'us (manuaalsed test-konteinerid, prod puutumata):**
- **PgBouncer (transaction-mode) + Medusa caveat SELGE:** boot+cart+päring OK, 0 prepared-statement viga (node-pg ei kasuta named prepared stmt). Warm 0.04s.
- **PgBouncer CAPIB pg-conn 20/100** kogu koormuse vältel (8+16 paralleelset, N instantsi) → connection-exhaustion LAHENDATUD.
- **Connection-math ilma PgBouncer'ita:** 2 replica't crashisid boot'il "pool is full" → PgBouncer kohustuslik.
- **Skaleerimine jaotab:** 8-paralleelne raske-päring 1 instants 25s → 2 instantsi 16s.
- **Taastumine:** burst → ~20-24s backlog-drain → täielik taastumine (pole püsiv kahjustus). → healthcheck-tolerants ~30s.
- POC-config: `edoburu/pgbouncer` transaction/pool20/scram + medusa DATABASE_URL→pgbouncer.

**Test-konteinerid koristatud** (pbtest1/2, pgbouncer eemaldatud). Staging puhas: Coolify-stack + xlmarket-redis-staging (Coolify-medusa kasutab seda). medusa /health 200, pg 11/100.

**Prod-rollout (avatud, vajab Tarmo go + koormustesti):** PgBouncer+replicad Coolify-compose-teenustena (mitte manuaalsed) + Traefik-LB + worker-instants + healthcheck-tolerants + DEFAULT_POOL_SIZE tuning k6-tipu järgi. Vt §7 Faas 3-5.

### ✅ Multi-variant hinna-verify enne Fix#1 prod-deploy'i (2026-06-07)

**Küsimus:** kas multi-variant toodete (eri hindadega variandid) hinnad oleksid Meili-hinnaga valed (Meili indekseerib ainult produkti-tasandi hinna)?

**DEFINITIIVNE vastus: probleemi EI OLE.** Kogu kataloog = **17105/17105 toodet täpselt 1 variant** (`product_variant` group-by, ">1 variant" päring TÜHI). VEVOR feed loob SPU-mudeliga 1 variandi/toode. → Fix #1 Meili-hind (produkti-tasandi) on **kogu kataloogi ulatuses õige**, sest 1 variant = 1 hind. Multi-variant teste pole võimalik teha (neid pole). **Fix #1 OHUTU prod-deploy'iks.**

**⚠ TULEVIKU-GUARD (kui uus tarnija toob multi-variant tooteid, nt Powermat):** Meili indekseerib AINULT produkti-tasandi `price` (mitte per-variant). Multi-variant toode → kõik variandid näitaks sama (valet) hinda. route fallback `variant?.calculated_price` ka undefined (calc_price dropitud getProduct'ist). **Lahendus enne multi-variant kasutuselevõttu:** (a) indekseeri per-variant hinnad Meili'sse, VÕI (b) route guard `if variants.length>1 → getProduct fetchib calc_price + kasuta Medusa per-variant hinda`. Lisa feed-sync'i assertion mis flag'ib kui multi-variant toode tekib (püüa enne kui hind katki).

### ✅ FIX #1 PROD-DEPLOY ÕNNESTUS (2026-06-07, samm 1b)

Prod (uo28ovobnflauslqjgxeohl0) deploy commit **0119c2d3** (rollback-commit: e0c25e57). **AINULT Fix#1 + env**, redis-mooduleid/PgBouncer EI (need = samm 3).

**Prod env seatud (Coolify):** LOG_LEVEL=error, DB_POOL_MIN=10, DB_POOL_MAX=50, NODE_OPTIONS=`--no-network-family-autoselection` (ilma --require → blocked-at preload OFF prod'is + ohutu build). Redis-mooduleid EI (in-memory).

**Eeltingimus:** prod Meili indekseerib hinna (price=973.94) ✅.

**Verify KÕIK ROHELINE:**
- medusa /health 200, LOG_LEVEL=error, POOL=50, redis=in-memory (0 redis-env), pg-conn 6/100
- avaleht 200 (0.005-0.06s), toote-detail **timing 0.008-0.06s**
- **public hind €973.94 Meili'st** (õige), variant.id, ET title/breadcrumb, 10 pilti, richDesc, 5 selling-point
- admin.xlmarket.ee 200, admin Orders 401 (auth, server elus)
- logid puhtad (ainult pre-existing ::1:5435 boot-probe, 3× boot'il, kahjutu — env'is pole 5435, DB töötab)

**Post-deploy sammud (CLAUDE.md, konteineri-nimi muutus):** medusa-admin.yaml + xlmarket-dev.yaml uuendatud uue konteineri-nimega + medusa→mailcow-võrk connect. (public xlmarket.ee = Coolify-label, auto.) Cold-start: esimene public-hit peale deploy'd 000:20-25s, siis stabiilne 200 (Traefik/Next soojendus).

**Stopgap (12s+retry) JÄÄB prod'is.** Fix#1 vähendab toote-detail query.graph koormust (kerge päring + Meili-hind). Cart-skaleerimine (PgBouncer+replicad) = samm 3, eraldi prod-infra otsus (vt tasks/2026-06-07-scaling...).

### ✅ SAMM 2a — BROWSE-CACHE (staging, app-tasand, 2026-06-07)

Commit a77a7f08 (storefront + feed-sync). Deploy'tud staging'usse + testitud. **Prod puutumata.**

**Tehtud:**
- **Browse-API cache-headerid:** header-categories (s-maxage=3600), products+search (s-maxage=300+SWR). product/[handle] juba s-maxage=3600.
- **Dünaamilised lehed no-store** (middleware): ostukorv/account/tellimus/login/register/vordlus → `private, no-store` (CDN-kaitse, KRIITILINE reegel).
- **Browse-lehed:** juba ISR revalidate=3600 (toode/kategooriad/avaleht/otsing). getProduct+getMeili fetch'id cached (revalidate 3600/300).
- **/api/revalidate endpoint** (secret REVALIDATE_SECRET): feed-sync invalidatsioon. feed-sync-bulk.sh [6/6] hook (gated STOREFRONT_URL+SECRET).

**Test-tulemused (staging):**
| Test | Tulemus |
|---|---|
| **8 paralleelset /api/product (cached)** | **0.014-0.067s** ✅ (oli 25s freeze → cache LAHENDAB concurrency!) |
| 8 paralleelset toode-leht | 0.087s enamus (full-route-cache HIT), max 9s (üks revalidation-render) |
| dünaamilised lehed Cache-Control | `private, no-store` ✅ (ostukorv/account/tellimus/login/vordlus) |
| revalidate-endpoint | 401 ilma secret, töötab secretiga (handle+all) ✅ |
| nav-kategooriad avalehel | 178 linki (SSoT) ✅ |

**Võti:** browse DATA cache (server-side ISR + s-maxage API) → **concurrent product-detail EI freezi Medusat** (0.014s vs 25s). 2a GOAL saavutatud.

**Märkused/caveat'd:**
- toode-leht response = `no-store` (Next 15 App Router vaikekäitumine — leht on SERVER-SIDE full-route-cached, Medusat ei taba; CDN-html-edge-cache = Faas 2/Cloudflare, eraldi samm).
- **header-categories API 500 "Category fetch timeout"** — PRE-EXISTING Medusa /store/product-categories aeglus (2731 kat query.graph >5s INFLIGHT timeout). **Nav EI katki** (MegaMenu kasutab SSoT category-tree.generated.json, 178 linki renderdab). Caching aitab kui õnnestub. Eraldi fix: tõsta getCategoriesCached timeout VÕI cache agressiivsemalt (sama query.graph-juur).
- Prod-deploy (samm 2a→prod): peale staging-OK, env REVALIDATE_SECRET+STOREFRONT_URL prod'i + deploy. EI veel tehtud.

### ✅ 2a viimistlus — header-categories robustseks (2026-06-07, staging)

Commit 779407ba. `lib/category-cache.ts` ümber: SWR + SSoT-fallback + timeout 5s→25s.
- **Juur:** /store/product-categories fetch ~4s (2731 kat), 5s INFLIGHT-timeout katkes vahelduvalt → cold-cache 500.
- **Fix:** (1) timeout 25s (soe fetch jõuab), (2) stale-while-revalidate (serveeri vana KOHE + bg-refresh), (3) SSoT-fallback (category-tree.generated.json → ProductCategory) kui Medusa-fetch fail + cache tühi → EI 500'a KUNAGI, (4) SSoT märgitakse stale'iks (väldi korduvat 25s ootust).
- **Test staging:** header-categories COLD **200** (3.9s real-fetch), warm 0.005s, 2731 kat/18 L1 ✅. Nav 178 linki, dünaamilised lehed ikka no-store (regression puudub).

### ✅ SAMM 2a → PROD (2026-06-07)

Prod (uo28...) deploy main `afcad7e4` (rollback-commit 0119c2d3). Browse-cache + categories-fix. Prod env lisatud: REVALIDATE_SECRET + STOREFRONT_URL (http://storefront:3030). Redis-mooduleid/PgBouncer EI (samm 3).

**Verify KÕIK ROHELINE:**
- medusa /health 200; logid puhtad (ainult 5435-probe)
- **KRIITILINE — dünaamilised no-store** (internal + läbi CF): ostukorv/account/tellimus/login/vordlus = `private, no-store`, cf-cache-status DYNAMIC (cart EI cache'itu) ✅
- browse-API: header-categories 200+s-maxage=3600 (categories-fix töötab, ei 500), products s-maxage=300, product/[handle] s-maxage=3600 ✅
- revalidate: 401 ilma secret, töötab secretiga ✅
- public läbi CF: /et 200 (0.28s), /et/toode 200 (0.27s), hind €973.94, nav 178 linki ✅
- Post-deploy: Traefik yaml (medusa-admin + xlmarket-dev) + mailcow-võrk uuendatud (konteineri-nimed muutusid).

**Midagi katki pole — rollback ei vajatud.** Stopgap (cart 12s+retry) jääb. Järgmine: CF cache-reeglid (browse-HTML edge-cache + purge feed-sync'il).

### ✅ CLOUDFLARE AKTIIVNE + VERIFY (2026-06-07 — NS-fix töötas)

Tarmo parandas .ee NS `edaard`→`edward`, propageeris → CF zone AKTIIVNE.
**Verify (prod läbi CF):** `/`→307 /et (locale, mitte redirect-loop), avaleht/cart 200, admin 200, **cf-ray olemas, server: cloudflare, SSL kehtiv** (Google Trust Services edge-cert).
**EMAIL terve (kriitiline kontroll):** MX (10 mh7 / 20 mh9.elkdata.ee), SPF (`v=spf1 a mx include:mail.spf.elkdata.ee a:mail.xlrent.eu ~all`), DKIM (dkim selector), DMARC (`p=none`) — KÕIK alles. Mail-host'id **185.7.x = DNS-only** (mitte CF-proxied 104.x/172.6x) → SMTP töötab.
- ⚠ Email-watch: SPF `a` resolveerib nüüd CF-IP-le → transaktsiooni-mail origin'ist (mailcow 65.21) otse saatmisel lisa 65.21 SPF-i (inbound + elkdata OK).
- admin.xlmarket.ee = DNS-only (cf-ray puudub, ei proxy'ta — OK).
- cf-cache-status = DYNAMIC (CF ei cache veel — cache-reeglid = järgmine samm peale 2a→prod).
- toode-leht vahelduvalt aeglane läbi CF (origin-render, no-store → CF ei cache; CF cache-reeglid lahendavad).

(Vananenud "BLOKEERITUD" staatus roadmap+scaling-spec'is uuendatud → AKTIIVNE.)

### 🔴 CLOUDFLARE ZONE PENDING — DIAGNOOS (2026-06-07, read-only DNS)

**Blokeerija TUVASTATUD: .ee NS-delegatsioonis KIRJAVIGA.**
- Register delegeerib (whois + `dig NS xlmarket.ee @.ee-TLD`): **`edaard.ns.cloudflare.com`** (KIRJAVIGA — ei eksisteeri, ei resolveeru) + `penny.ns.cloudflare.com` (õige).
- Cloudflare ootab: `edward.ns.cloudflare.com` + `penny.ns.cloudflare.com`.
- `edaard` → not found; `edward` → 172.64.35.210 (õige CF NS).
- Cloudflare nõuab MÕLEMAT määratud NS-i korras → kuna `edaard` vigane, zone jääb **Pending**, ei aktiveeru.

**DNSSEC EI blokeeri:** DS-kirjed @.ee TLD puuduvad, DNSKEY puudub (zone pole signed).

**Parandus (Tarmo, .ee registrari paneel — Code EI saa registrit muuta):** `edaard.ns.cloudflare.com` → `edward.ns.cloudflare.com`, `penny` jätta. NS TTL propagatsioon ~6h → Cloudflare auto-aktiveerib. Seejärel A-rekord Proxied + edge-cache + purge feed-sync'il (Faas 2 punkt 3).

**Mõju:** Cloudflare edge-cache (roadmap Samm 2 / scaling Faas 2 punkt 3) OOTAB seda parandust. App-tasandi browse-cache (2a) on tehtud ja ei sõltu Cloudflare'ist.

### ⚠️ STAGING Montonio-võtmed PUUDUSID Coolify env'ist (avastatud restartil)
Staging crash-loopis restartil: "Montonio: accessKey ja secretKey on kohustuslikud". Coolify env'is POLNUD `MONTONIO_ACCESS_KEY/SECRET_KEY` — vana konteiner töötas käsitsi-süstitud runtime-env'iga mis EI olnud Coolify'sse salvestatud → iga restart/redeploy oleks staging katki teinud. **Parandatud:** taastatud prod-konteineri sandbox-võtmetest → lisatud staging Coolify env'i (MONTONIO_ACCESS_KEY ff600802..., SECRET_KEY 44-märki, ENV=sandbox). Staging boot OK.

## KRIITILISED VEAD (leitud + seis)

### 1. "Failed to create cart" — cart-loomine flaky 5-12s ⚠️ STOPGAP (root cause lahendamata)
- Sümptom: POST /store/carts vahelduvalt 5-12s (http-log 5497ms), ~pooled kiired (0.1s). Storefront proxy 3s timeout → abort → "Failed to create cart".
- **Stopgap LIVE:** medusa-proxy timeout 3s→12s + cart-route 2×13s retry → 6/8 õnnestub, 504-d kadusid, 2/8 ikka >26s.
- **VÄLISTATUD:** DNS (lookup <8ms), pg-pool (0 leak), redis-churn (stabiilne), conntrack (1%), base-image (värske node-konteiner kiire), autoSelectFamily (flag deployed, vead 0), Montonio (aeglane ka ilma), mailcow-võrk (lahutamine ei aidanud), host-võrk (coolify-proxy→medusa 0ms).
- **SUUND:** medusa cart-WORKFLOW ootab ~5s async'i (event-loop vaba — /health+regions kiired). Tühi cart = absurdne 5s. Kahtlus: Medusa 2.x inmemory workflow-engine polling VÕI cart async-step (payment-collection/tax automatic_taxes). **Järgmine: async_hooks profiling.**

### 2. "Products temporarily unavailable" (salon-spa) ✅ LAHENDATUD
- Põhjus: kategooria-leht külm ISR-render deploy/medusa-boot aknas (cache'imata → aeglane → timeout). Ei olnud Meili (0.1s) ega tühi kategooria (309 toodet).
- Fix: `generateStaticParams` L1+L2 (810 lehte pre-render) → ei teki külma-renderit. Live, salon-spa nüüd 0.05s.

### 3. Breadcrumb inglise keeles ✅ LAHENDATUD
- Põhjus: `getBreadcrumbTrail` ei andnud `nodeName`-le locale't → vaikimisi name_en (kuigi name_et olemas kõigil 2731 sõlmel).
- Fix: getBreadcrumbTrail + 3 kutsujat (kategooriad/toode page + api/product route) annavad locale. Live ("Kodu", "Salong, spaa...").

### BOONUS — Montonio hostname VALE (KRIITILINE) ✅ LAHENDATUD
- `sandbox-api.montonio.com` / `api.montonio.com` EI EKSISTEERI (ei resolveeru). Õige = `sandbox-stargate.montonio.com` / `stargate.montonio.com` (Orders API v2 "Stargate" gateway). Ilma selleta makse poleks KUNAGI töötanud. Fix deployed.

## TÕLKED (täielik seis)
- **Toote-tõlked title_et/description_et:** ✅ 100% (17441).
- **Kategooria-nimed name_et + breadcrumb:** ✅.
- **messages/et.json (header/nav):** ✅ 234/234, 0 puudu.
- **1. UI-pass (06701e8b):** cart/checkout/tooteleht/filtrid/footer/konto/võrdlus/lemmikud/MegaMenu.
- **2. UI-pass (a14569c6)** — 1. pass JÄTTIS VAHELE: avaleht ("Explore Categories"→"Sirvi kategooriaid"), SearchBar ("Searching..."→"Otsin..."), login+register (21 str), ärikliendile (B2B vorm+optionid), alustajale.
- **Jääb:** placeholder "Min/Max" (OK, lühendid); **xl-admin paneel inglise** (madalam prio, CLAUDE.md tahab ET admin'i — eraldi task).

## Cloudflare 🟡 (proxy EI aktiivne)
- NS delegeeritud ✅ (edward+penny.ns.cloudflare.com globaalselt).
- Proxy HALL — autoriteetne NS tagastab origin IP 65.21.126.235 (A-rekord "DNS only", mitte "Proxied"). Kasutaja proovis 3× dashboardis, ei jää oranžiks → **zone võib "pending"/vale konto** olla.
- Mul POLE CF API-tokenit. Kasutaja andis zone ID `04661523023085812102ba1064d79bd375` (34 märki — CF ID on 32, KONTROLLI). Vaja CF API-token (Zone.DNS:Edit) → siis `PATCH dns_records {proxied:true}`. Sait töötab (SSL OK), proxy = lisaväärtus.

## Feed-sync bulk skript ✅ VALMIS (commit c1c88bb6), EI ole veel cron'ina
- `backend/scripts/feed-bulk-price.mjs` — VEVOR xlsx → hinna-diff → bulk SQL UPDATE (möödub Medusa-API'st → väldib 8s/toode Meili-subscriber aeglust). Dry-run OK (16117 SKU).
- `sync-synonyms.mjs` — synonyms taastus DB-st peale reindeksit.
- `feed-sync-bulk.sh` — orkestraator (lae→price→cache→reindex→synonyms).
- Dockerfile COPY-b need + healthcheck 300→600s. exceljs+pg juba deps.
- **TEGEMATA:** Coolify Scheduled Task registreerimine (`0 */4 * * *` → `bash /app/scripts/feed-sync-bulk.sh`) + käsitsi 1× test.

## Kategooriad
- 2731 sõlme SSoT (L1:18, L2:332, L3:892, L4:1075, L5:371, L6:42, L7:1). DB 3360 (~629 legacy pole SSoT-s). Faas A+B tehtud.
- **Leitavus 99.97%:** 32 kategoriseerimata toodet → L1 määratud (märksõna-mapping). l1_slug NULL 38→6. Meili 17441 docs (kõik otsitavad). Sügavus EI blokeeri (taxonomy.ancestors filter).
- **⚠ vevor-to-v3.json l1_defaults STALE** — viitab vanadele handle'tele (`fitness-sports-recreation` vs praegune `fitness-sports-games`, `plumbing-water-systems` puudub) → uued tooted neis VEVOR-L1-des ei kategoriseeru. **Vaja sünkida 18 praeguse L1-handle'ga.**

## Muu TEHTUD
- maintenance-konteiner eemaldatud (kaaperdas xlmarket.ee priority=200). HTML /var/www + /tmp backup.
- MEDUSA_BACKEND_URL=https://admin.xlmarket.ee (Coolify env) → webhook.
- NODE_OPTIONS=--no-network-family-autoselection (medusa+storefront).
- **Uptime-Kuma:** xlmarket.ee + admin.xlmarket.ee monitorid lisatud (uptime-kuma-elibpgyt..., id 4+5, pingivad UP). DB-insert + restart.
- **Restore-test ✅** (96M backup → 149 tabelit, 17446 toodet, 0 viga).

## Deploy-protseduur (KORDUB igal deploy'l)
1. `curl -H "Authorization: Bearer $(cat /tmp/coolify-token.txt)" "http://localhost:8000/api/v1/deploy?uuid=uo28ovobnflauslqjgxeohl0"`
2. Medusa boot ~8.5 min (514s); healthcheck start-period 600s.
3. **Post-steps:** `docker network connect mailcowdockerized_mailcow-network <uus-medusa>` + Traefik yaml (medusa-admin.yaml + xlmarket-dev.yaml) konteineri-nimed coolify-proxy's.
4. DB/Meili IP-d muutuvad → re-derive `docker inspect`.

## JÄRGMISED SAMMUD (prioriteedi järjekorras)
1. 🔴 **Cart async-stall profiling** — checkout + Montonio usaldusväärsus sõltub. async_hooks/workflow step-timing.
2. 🟡 **Cloudflare proxy** — kasutajalt CF API-token VÕI dashboard kustuta+loo A-rekord Proxied'ina; kontrolli zone-staatus.
3. **vevor-to-v3.json l1_defaults sünk** (18 praeguse L1-handle'ga) — muidu uued tooted ei kategoriseeru.
4. **Feed-sync Coolify Scheduled Task** registreerimine + test.
5. **Montonio sandbox e2e test** (pärast cart-stall'i — muidu makse aeglane).
6. **Powermat:** powermat-to-v3.json mapping + pipeline rewire + import.
7. **Faas C** (900 kat 1-4 tootega) — PÄRAST Powermati.
8. Post-launch: xl-admin ET-tõlge, FAAS 2 live-UI, legacy 629 DB-kat koristus.
