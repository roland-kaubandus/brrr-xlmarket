# Sessioonilogi 2026-07-23 — XL

## Churned→OOS revert — juur + parandus (A+B+C)

**Sümptom:** Tarmo leidis "laos" pildi/sisu-vaese toote. Kontroll → KÕIK 18062 `in_stock=true` (eile OOS 6420). Churned→OOS vaikselt tagasi pöördunud.

**Juur:** k33g konteineris `FEED_CACHE_PATH=/data/...`, aga `/data` volume oli **root:root 755** ja app-user=uid 1001(medusa) → keegi ei saanud cache't sinna kirjutada → tühi. Eilsed 3 konteineri-reindeksit (956/spec/reprice) jooksid tühja cache'iga → guard `isOosFromFeed` tagastas kõigile `false` → kõik laos. `feed-sync.sh` on host bare-metal (ei jookse k33g-l).

**Mis veel pöördus:** ainult `in_stock`. `specs`/`filter_tokens` langevad `meta.dimensions` peale (terved); `compare_specs`/hind/kategooriad DB-st (terved).

**A — taastatud:** container-native refresh → OOS **6614**, konkreetne toode `in_stock=false`.
**B — guard abort:** `index-meilisearch.mjs preflightFeedCache` → tühi cache = exit 2 ENNE indeksit (override `ALLOW_EMPTY_FEED_CACHE=1`). Testitud.
**C — püsiv:** `build-vevor-feed-cache.mjs` austab `FEED_CACHE_PATH`/`FEED_XLSX_PATH`; uus `refresh-feed-cache.sh` (download→build→reindeks) = Coolify Scheduled Task (medusa, `sh /app/scripts/refresh-feed-cache.sh`, `0 */4 * * *`). Container-native (MITTE host-copy) → cutover-kindel.

**Ühekordne:** `/data` chown medusa:nogroup (uid 1001) — volume püsib redeploy'de üleselt.

**Deploy:** skriptid cp'd konteinerisse (aktiivsed kohe); git = püsiv allikas (redeploy toob image'ist). Coolify Scheduled Task registreerimine = Tarmo (dashboard). Detailid: `reports/churned-oos-juur-2026-07-23.md`.

## Scheduled Task file-not-found + feed-sync-bulk hinna-konflikt (järelkontroll)

**File-not-found juur:** `backend/Dockerfile` runner teeb **valikulise per-fail COPY** (ei kopeeri `scripts/` tervikuna) → `refresh-feed-cache.sh` polnud loends → ei jõudnud image'i. **Parandus:** lisatud Dockerfile COPY-ritta.

**⚠️ feed-sync-bulk.sh EI ole cron-ohutu (uus leid):** selle `feed-bulk-price.mjs` = flat MAP×1.15, aga eile ühtlustati kogu kataloog **pricing-engine'ile** (`computeRetail`, `reprice-existing.mjs` 34f07615). Vahe +396…+1009 c (kerged) / −990 c (rasked) → cronina revertiks hinnad iga 4h. Lisaks curl puudub + `set -u` CF-unbound. **Otsus:** feed-sync-bulk.sh jääb repos (tulevik), aga cron = **refresh-feed-cache.sh** (stock-only, hinna-vaba, wget). Kui feed-bulk-price kunagi pricing-engine'ile viia → siis feed-sync-bulk taas kaalumisel.

**⚠️ Image ei sisaldanud commit 3ee1d4c9:** 09:00 redeploy build'is vanast koodist (Coolify cache). **Järgmine redeploy peab build'ima värskest commit'ist** (force rebuild vajadusel).

**Tarmole:** Scheduled Task = `sh /app/scripts/refresh-feed-cache.sh` (medusa, `0 */4 * * *`); redeploy värskest build'ist ENNE seda. **ÄRA** pane feed-sync-bulk.sh cronina (hinna-revert). Dockerfile-fix commit järgmine.

## Arhiveerimis-loogika (churn'inud tooted → SEO-säilitav delist)

**Probleem:** 3242 churn'inud (feedist kadunud, OOS) toodet jäid kataloogi hõredate lehtedena.

**Vanuse-jaotus (näita-enne):** kõik churned = üks **aprilli-batch** (103p vana). created_at >90p = 3249; updated_at SAASTUNUD eilsete bulk-op'idega (kasutu). `last_seen_in_feed` olemas: 0 → väli nullist. Range hõre-heuristik (≤1 pilt & desc<40) tabas ainult 1 → "hõredus" = tõenäoliselt puuduv ET-tõlge, mitte DB-tühjus.

**Ehitatud (6 punkti):** `feed-status-stamp.mjs` (last_seen_in_feed + feed_status in_feed|missing|archived; bootstrap missing→created_at ühekordne; tagasitulek=SKU-järgi un-archive EI loo uut) · `archive-proposals.mjs` (>90p raport + väravatud `--execute` delist) · `index-meilisearch.mjs` filter `feed_status IS DISTINCT FROM 'archived'` · `refresh-feed-cache.sh` samm 3/4 stamp · Dockerfile COPY.

**SEO-säilitus:** archived jääb Medusa `published` → toote-API loeb `getProduct` Medusa'st (Meili fallback) → LEHT+URL 200. Ainult Meili jätab vahele → kaob otsingust/listingust. Pööratav.

**Jooksu-tulem:** stamp 18061 (in_feed 14815, missing 3246, custom 1); archive-kandidaate >90p **3242**. `--execute` **EI jooksutatud** — ootab Tarmo kinnitust. Detailid: `reports/arhiveerimine-2026-07-23.md`.

## "Success 58s, null tegevust" — refresh-feed-cache FAIL-LOUD kõvendus

**Sümptom:** Scheduled Task raporteeris "Success 58s", AGA /data puutumata (xlsx 08:12, cache 12:00, task 09:43) ja Meili viimane reindeks 12:00:54 UTC — 09:43 EI reindekseerinud. Roheline tuli, null tegevust (sama muster mis feed-sync 3 kuud + guard "töötas").

**Diagnoos:** 58s ≈ täis-ahela aeg (verifitseeritud päris-jooks ~64s) → task jooksis tõenäoliselt täies mahus, aga **ephemeral/vale konteineris**, mille /data kadus; exit maskiti 0-ks. Lõplik Coolify-põhjus (one-off vs exec, mount) vajab dashboard'i.

**Parandus — `refresh-feed-cache.sh` iga samm tõestab tulemuse:** `[prep]` /data kirjutatav · `[1/4]` wget rc + ≥1MB + PK-magic + mv · `[2/4]` cache mtime ≥ RUN_START + ≥10k SKU + ei kukkunud >20% vs eelmine · `[3/4]` stamp rc · `[4/4]` reindeks rc + doc-count ≥15k + **Meili viimane reindeks-task finishedAt ≥ RUN_START** (jagatud Meili = ephemeral EI võltsi). Lõpp: cache mtime ≥ RUN_START. Iga lünk → exit != 0 → Coolify "Failed".

**Testitud:** päris-jooks exit 0 (cache 14827 SKU, reindeks 18062 docs 32s, kõik väravad, ka parandas /data staleness'i). TEST A (vigane URL) → exit 1 [download]; TEST B (kirjutamatu /data) → exit 1 [prep]. Mõlemal trap-teade "Coolify peab näitama Failed".

**Tarmole:** (1) redeploy taxonomy-v4 värskest build'ist (skriptid image'i). (2) Scheduled Task PEAB jooksma medusa SERVICE-konteineris (õige /data named-volume), MITTE ephemeral one-off. Meili-värav kaitseb kasutaja-mõju sõltumata; õige mount = cache püsib. Detailid: `reports/churned-oos-juur-2026-07-23.md`.

## Split-brain saadavus — kaks tõe-allikat lahknesid (õhtu, D-etapp)

**Sümptom (Tarmo KRIITILINE):** kaart (Meili)="Otsas", tooteleht (Medusa)="Laos"+ostunupp → klient sai tarnimatu toote osta.

**Juur:** kaart luges Meili `in_stock` (õige), tooteleht `ProductPurchasePanel.hasInventory()` → `variant.inventory_quantity` (Medusa dummy=100, ei täitunud kunagi) → alati "Laos". Cart/checkout tegi **null laoseisu-kontrolli** = launch-blokeerija.

**Arhitektuur-otsus:** ÜKS ülemine tõde (feed-cache `bySku`) → jagatud `lib/feed-stock.mjs::isOosFromFeed()` → KAKS sünkroonis projektsiooni: Meili `in_stock` (kuva) + Medusa `stocked_quantity` (jõustus). Enne oli Medusa inventory sõltumatu dummy → lahknes.

**Parandus (commit `eda090bf` main / `7d9ce397` v4):**
- `lib/feed-stock.mjs` UUS — jagatud otsus (impordib nii Meili kui sync).
- `index-meilisearch.mjs` — kasutab jagatud otsust (eemaldatud lokaalne koopia).
- `sync-medusa-inventory.mjs` UUS — churned/OOS→stocked=0, tagasitulek→100, feed-managed only, --dry-run, tühja-cache preflight exit 2.
- `route.ts`+`ProductContent.tsx`+`ProductPurchasePanel.tsx` — tooteleht loeb Meili `in_stock` (`inStock = feedInStock!==false && hasInventory`).
- `refresh-feed-cache.sh` [4/5] sync-inventory wire (Meili+Medusa värskuvad koos). Dockerfile COPY.

**Live jõustatud:** 5666 churned/OOS → stocked=0 (bug-toode rattan-kummut kinnitatud `0`). `manage_inventory=true` juba kõigil → 0 = Medusa keeldub add/checkout = **server-poolne blokk aktiivne** (ei vaja manage_inventory-flip'i).

**⏳ Kuva-fix vajab Coolify redeploy** (build-time bundle). Server-blokk (inventory=0) juba live.

## Sisu-kvaliteet (LISAKS — korrigeerib varasemat "gallery-only" järeldust)

Feedis+laos 11485 toodet. Dimensioonide kaupa:
- **0 galeriipilti: ainult 38** (3 aprillist) — galerii-pildid on OK (varasem järeldus õige).
- **0 selling_point: 7965** (7246 aprillist) — **SUUR lünk.** Enamikul aprilli-toodetest puuduvad selling-point'id.
- **tühi rich-kirjeldus: 2742.** tühi base-kirjeldus: **0** (kõigil on baaskirjeldus → leht pole katki).

Näidised: gal=10, sp=EI, descr=596 → pildid+baaskirjeldus olemas, aga selling-point-bulletid puuduvad → näeb tühjem välja kui 956-import (millel sp 1–5). **Parandab ainult `import --update` / spec-ekstraktsioon selling-point'ide peale** (pole 3 kuud jooksnud). EI ole launch-blokeerija (leht töötab), aga kvaliteedi-lünk.

## Sisu-lünk — pre-flight kukutas "pooletunnise impordi" (2026-07-23 õhtu)

**2 eksitust järjest parandatud pre-flight-kontrolliga:**
1. **Vale väli:** "7965 ilma selling_point'ita" luges `selling_point_1` skalaari — page loeb `selling_points` ARRAY-d (route.ts:407). Päris array-lünk = **839**. Rich: page loeb `sanitized_rich_description` (route.ts:426), tühi = **4342** (mitte 2742).
2. **Feed ei paranda rich'i:** VEVOR xlsx kärbib `description_html` Excel 32767 cell-cap'i juures → **32% (4765) = 30000 märki CSS slider-boilerplate't** (identne hash baf2b923), päris sisu kärbitud. Re-derive andis IDENTSE junki (pre-flight: 6/6 sample sama hash). 4342-st feed parandab ainult **13** (1054 pole feedis · 3275 CSS-junk).

**Tulem:** `backfill-content-fields.mjs` parandaks ~14 toodet = no-op. Rich-lünk (4329) = **generaatori/alt-allika töö** (xlsx-feed EI ole generaatori sisend — kärbitud). EI jooksutatud `--execute`.

**Skript-fix (KRIITILINE):** lisatud `isJunkRich` kvaliteedi-värav — ilma selleta oleks `--execute` kirjutanud 3275 CSS-junk-rida. Allowlist-värav + junk-värav mõlemad passeerivad dry-run'is (14 muutuks, ainult 4 metadata-võtit).

**Õppetund:** mõõda ALATI väljal mida leht renderdab; sisu-fixil kvaliteedi-pre-flight ENNE live-kirjutust.

## Archive-proposals --execute + Coolify redeploy toimus samal ajal (2026-07-23 õhtu)

**Archive:** `backend/scripts/archive-proposals.mjs --days 90 --execute` → **3242 toodet** `feed_status='archived'` (kadunud 90-103p, ~aprillist). Leht+URL jäävad elus (Medusa published, SEO säilib), ainult Meili delist.
- Meili reindeks (`index-meilisearch.mjs`) upsertib AINULT (ei kustuta stale) → indeks jäi 18062. **Käsitsi delete-batch** 3242 archived id → **Meili 14820 docs** (õige). *NB: index-meilisearch.mjs EI kustuta archived/deleted docs — tuleviku-parandus vajalik (delete-batch samm).*

**Coolify redeploy k33g (Tarmo, samaaegne):** kogu k33g-stack restartis ~21:40 (uued container-instance-id'd, sama app-id). **SOURCE_COMMIT=7d9ce397 = taxonomy-v4 tip → split-brain display-fix (feedInStock gate) nüüd LIVE** (kinnitatud .next buildis). Pending carryover "Coolify redeploy display-fix" = TEHTUD.
- **Andmed + Meili delist ÜLE ELASID restarti** (k33g db archived=3242 intact, Meili 14820 docs). Restart EI wipe'nud indeksit.

**⚠️ ORPHAN STACK `uo28ovobnflauslqjgxeohl0`:** eraldi 3-nädalane Coolify deploy (19.06), STALE db (17446 toodet, feed_status kõik null — ilma feed-status/split-brain/archive tööta). Pole live-siht (mina töötasin k33g-l). **Tarmole: kas uo28 on teadlik/vajalik? Kui ei → koristada (ressurss + segadus-risk kui proxy sinna satub).**

**PARANDUS (Tarmo):** uo28 EI ole orphan — see on **PROD** (xlmarket.ee, coming-soon režiimis, teadlikult külmutatud kuni cutover). k33g = **staging** (kus kogu töö). ÄRA koorista uo28. Prodi 3-kuu-vanadus (feed_status null, 0 tänast tööd) = **cutover-blokeerija** (juba nimekirjas). Topoloogia mällu: [[stack-topoloogia-prod-staging]].
