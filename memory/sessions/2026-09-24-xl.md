# 2026-09-24 — XL sessioon (Review-bucketi nähtavus)

## Tehtud

### SAMM 0 — Review-bucketi kaardistus (ainult lugemine, 0 muudatust)
- **2 elusat ämbrit:** `synonym_review` (3828 pending, ~99% piiripealne-hea, 9 tõelist prahti conf=0.00) +
  `classification_review` (26 pending: 14 new_l3, 9 review, 3 quarantine).
- **1 SURNUD pärand:** `v_review_queue`/`category_classification_audit` (11505 distinct, viimane kirje
  2026-07-22, cron ei toida) → EI kasuta digestis. Loeb veel: `backend/.../categorization-queue/route.ts`
  (elus xl-admin leht surnud andmete peal — samm 2 UI juures otsustada asendus/peit),
  `drain-review-queue.mjs` (käsitsi), INV skipib.
- **Olemasolev digest leitud:** `pipeline-review-digest.mjs` katab AINULT classification_review, on JUBA
  öös ühendatud (`import-pipeline.sh:296 --telegram`) — polnud "välja kommenteeritud" (cron rida 20 =
  dokumentatsiooni-kommentaar faili sünnist 7ad5cc9c).

### SAMM 1 — Digest kata mõlemad ämbrid (commit taxonomy-v4=c820abd4, main=c09688e6)
- `pipeline-review-digest.mjs` laiendatud (+145/−61): synonym_review sektsioon (arv + kindluse-jaotus
  0.00/<0.7/0.7-0.85/≥0.85+review:true + top-5 L3-klastrit), lävend-signaalid (vanus / kasvu-trend
  7p-vs-baas / lahendamata-suhe; span<30p → "andmeid veel vähe", ei fabritseeri rohelist).
- **Link env XL_ADMIN_BASE_URL + fail-loud** (puudu → ei kasuta prod-domeeni) jäävad koodi, aga
  rida EI KUVATA (`void linkLine`, kommentaar "link aktiveeritakse sammus 2" kui xl-admin/review-bucket leht valmis).
- **Masinloetav `REVIEW-BUCKET — N` rida** (LOG, mitte Telegram) → cron STATUS review_waiting = kogusumma.
- `import-pipeline.sh` OK-rida "Review-bucketis ootab: N" = **koos synon+klass (3854)**, mitte ainult klass.
- **Öine jooks saadab ÜHE digesti** (mõlemad ämbrid) + eraldi pipeline-OK-kokkuvõtte (alati olnud, erinev eesmärk).
- Cron/pipeline struktuuri EI muudetud (ainult OK-rea dbq ühtlustus).

### SAMM 2 otsused + 2c ehitus (EI COMMITITUD — ootab Tarmo kinnitust)
- **Plaan uuendatud** (`reports/review-bucket-ui-plaan.md`) 6 otsusega: valim 100 / VALE ≤ 3/100 / show-all-if-band<N /
  lävi+valim seadistatavad · VALE-definitsioon UI-s + 2 näidet · 1330 SAMA lävi (mitte leebem) ·
  sync öine [7.5]+"sünkri kohe" · vana categorization-queue pärand-bänner · järjekord 2c→2b→2d.
- **PUNKT 4 sync-kontroll:** `sync-synonyms.mjs` = **`PUT settings/synonyms`** täis-replace DB `product_synonym`-ist.
  PUT ASENDAB kogu seade → DB-st kustutatud sünonüüm kaob Meilist automaatselt. + öine [7] reindeks pühib,
  [7.5] ehitab DB-st. **→ eemaldus töötab, EI vaja parandust enne 2b-d.**
- **2c ehitatud** (uued failid, tsc-puhtad):
  - `backend/src/api/admin/review-bucket/route.ts` — GET kontsepti-klastrid+DUP+logi; POST assign_existing/create_l3/
    quarantine/reject/undo. review_decision_log (append-only) ensure. **propose-not-create:** create_l3 EI lisa
    kategooriat live (salvestab approved_pending_build → struktuuri-build teeb tegeliku loomise+SSoT+4-sammu).
  - `storefront/app/api/admin/review-bucket/route.ts` — proxy (readAdminSession gate → medusaAdminFetch).
  - `storefront/app/xl-admin/review-bucket/page.tsx` + `ReviewBucketClient.tsx` — klastri-UI, DUP-värav (raadio,
    paigutus-nupud lukus kuni ÜKS kodu valitud), otsuste-logi + [Võta tagasi].
  - `layout.tsx` — nav-link "Review-bucket" lisatud.
- **DUP-värav valideeritud 26 real:** lumber rack n=9 (new_l3+quarantine, 3 kodu) 🚪DUP · paracord rope n=3
  (new_l3+quarantine) 🚪DUP · ülejäänud non-DUP. ✓
- **Test-otsus + undo staging'us (tõestus):** paracord quarantine `prod_01M2Y5BB...PS0K` (kodutu) → assign_existing
  trossikoied → cr=resolved, logi #1 applied → undo → kodutu taastatud, cr=pending, logi undone. pending 26→26. ✓
  **Gotcha:** kodutu toode → prev_category_ids=`[]` (route JS `||[]` õige); undo peab olema null/empty-safe.
- **review_decision_log tabel jäi staging'usse** (funktsioon vajab); demo-logirida kustutatud, seis pristine.

### SAMM 2c REVISJON (Tarmo 1f0a — kinnitatud, paranda enne commit'i)
- **1a digest** (`pipeline-review-digest.mjs`): uus rida "🏗 Kinnitatud, ootab struktuuri-buildi: N toodet / M L3 · vanim Xp"
  + top-6 L3 nimekiri + masinloetav rida `ootab-buildi N`. Loeb `review_decision_log`
  (action=create_l3, status=approved_pending_build, undone_at IS NULL). build.n=0 → sektsioon peidus.
- **1b UI "Ootab buildi"** (ReviewBucketClient): ERALDI amber-sektsioon üleval (tabel: uus L3 · vanem-L2 · tooteid
  · vanus · näited · [Võta tagasi]). **create_l3 → cr.status='approved_build'** → tooted LAHKUVAD pending-klastritest,
  EI ilmu otsustamata. GET `pending_build` päring (log + product-titles).
- **1c RAPORT (ei ehitatud):** struktuuri-build = KÄSITSI genyM.mjs + lock-harness.mjs + 4-sammu deploy.
  Cron EI loo L3. **PUUDU: build-bridge** — miski EI loe `approved_pending_build` logi ega määra tooteid
  uude L3-sse pärast loomist. Praegu inimene määraks käsitsi. Vaja: build-samm loeb logi → loob L3 → assign.
- **2a "muu olemas-L3"** — CatSearch komponent (GET `?search=`, leht-L3 = ilma lasteta, ILIKE nimi/handle),
  4. raadio-valik iga klastri all, sama DUP-lukk. Backend GET `?search=` haru + proxy edastab.
- **2b väljade parandus** (JUURVIGA leitud): `proposed_l3`=lähim OLEMAS-L3, `suggest_name`=uue L3 nimi,
  `suggest_l2`=vanem-L2 (EBAUSALDUSVÄÄRNE: vahel handle, vahel label "Ladu > Riiulid ja restid").
  **Vana UI luges proposed_l3 "olemas-koduks" ja VARJAS uue-L3-ettepaneku** → lumber rack näitas
  "olemas: seinapaneelid-pegboardid" (proposed_l3), digest "→@Ladu>Riiulid" (suggest_l2). **Fix:** üks member
  annab nüüd MÕLEMAD kandidaadid; uue L3 all näidatakse ERALDI pakutud vanem-L2 (resolveeri handle, muidu
  "vali käsitsi") JA lähim olemas-L3 (DUP-hoiatus, klõps → valib selle). suggest_l2 label → create_l3 nõuab
  käsitsi L2-valikut (l2Pick).
- **3 migratsioon** `scripts/migrations/007-review-decision-log.sql` (idempotent IF NOT EXISTS + 2 indeksit).
  route.ts ensureLogTable = turvavõrk. Rakendatud staging'usse (indeksid lisatud).

### SAMM 2c REVISJON — commit + deploy + päris-test (1f0a punktid 4-5)
- **Commit mõlemale harule:** `origin/main=ada88604`, `origin/taxonomy-v4=aad66431` (Coolify buildib v4).
  Push OK, k33g redeploy TRIGGERITUD + **DEPLOY_OK** (Coolify deployment vdp8yg2dl243fkjefowxqlby, 18 sammu finished).
- **🔴 KRIITILINE BLOKEERIJA LEITUD (eelnev, mitte selle muudatuse põhjustatud): deploy'tud storefront-konteineris
  puuduvad KÕIK xl-admin env-muutujad.** Kood loeb:
  - `lib/admin-session.ts` → `ADMIN_SESSION_SECRET` (getSecret THROW kui puudub), `TARMO_ADMIN_EMAIL`, `TARMO_ADMIN_PASS` (login) — **kõik PUUDU**.
  - `lib/medusa-admin.ts` → `MEDUSA_ADMIN_EMAIL`, `MEDUSA_ADMIN_PASSWORD`, `MEDUSA_BACKEND_URL` — **kõik PUUDU** (BACKEND_URL fallback 127.0.0.1:9001 = vale, backend eraldi konteiner).
  - Coolify annab hoopis: `EMAIL_ADMIN`, `MEDUSA_ADMIN_PASS`, `NEXT_PUBLIC_MEDUSA_URL=http://medusa:9000`, `SERVICE_URL_MEDUSA=https://staging-api.xlmarket.ee`.
  - **Tagajärg:** login võimatu (getAdminUsers=[] → checkCredentials null; signAdminToken THROW) → **kogu xl-admin sektsioon (sh review-bucket) EI AVANE deploy'tud staging'us.** Env-nime lahknevus + väärtuse-lahknevus.
  - **Lisaks:** `MEDUSA_ADMIN_PASS` väärtus konteineris **ei autendi** ühtegi admin-kasutajat (401: tarmo@naissaar.eu / admin@xlmarket.eu / tarmo@xlmarket.eu). emailpass identiteedid DB-s: admin@ · tarmo@xlmarket.eu · test-auth@ · xl-import@.
- **Päris-test (punkt 4) BLOKEERITUD:** ilma töötava admin-paroolita ei saa deploy'tud API-t autentida. Ühekordse
  staging-admini loomine (`npx medusa user` — TOIMIS) + auth-probe said **permission-denied** (auto-mode klassifikaator);
  ei püütud sellest mööda. Test-kasutaja `rb-test@xlmarket.eu` **koristatud** (provider_identity DELETE + user deleted_at) → staging pristine.
- **Seis pärast (pristine, kontrollitud):** classification pending **26** · synonym pending **3828** · review_decision_log **0** rida · pending_build **0**.
- **JÄRGMINE (vajab Tarmot — saladused + Coolify env, ei saa autonoomselt):** seada k33g storefront env:
  `ADMIN_SESSION_SECRET` (≥24 tähem.), `TARMO_ADMIN_EMAIL`+`TARMO_ADMIN_PASS` (UI-värav), töötav `MEDUSA_ADMIN_EMAIL`+`MEDUSA_ADMIN_PASSWORD`
  (või kood loeb `EMAIL_ADMIN`/`MEDUSA_ADMIN_PASS` — vajab valiku-otsust) + `MEDUSA_BACKEND_URL=http://medusa:9000`.
  Alternatiiv: lisan koodi env-nime fallback'id (EMAIL_ADMIN/MEDUSA_ADMIN_PASS/NEXT_PUBLIC_MEDUSA_URL), AGA parool peab ikka
  töötama (praegu 401) → Tarmo peab õige parooli andma/lähtestama. **Kuni env seatud: leht ei avane, päris-test tegemata.**

## Pooleli / järgmine
- **🔴 xl-admin deploy-env (vt ülal)** — blokeerib review-bucket lehe avanemise + päris-testi. Tarmo otsus + saladused.
- **SAMM 2:** `xl-admin/review-bucket` leht (klastri-ülevaatus UI, kinnita/lükka klastri-kaupa) → siis
  aktiveeri digesti link (eemalda `void linkLine`, kuva `linkLine`). Otsusta ka surnud
  `xl-admin/categorization-queue` (v_review_queue) saatus.
- **HILISEM:** #3 glossary uus-termin hook · Outlet ristkuvamine · Outlet L2-pildid.

## Gotchad
- Digest oli juba pipeline'is live → digesti laiendus tähendab, et **järgmine öine jooks saadab uue
  sõnumi AUTOMAATSELT** (eraldi cron-ühendust polnud vaja).
- classification_review mõnel real `suggest_name` tühi (griddle/lumeader) → digest kasutab nüüd
  title-fallback'i, muidu klaster oli "?".

---

## SAMM 2c — xl-admin env BLOKEERIJA lahendatud (1f0a tee A) — õhtu

**Tarmo otsus 1f0a:** tee **A (õiged env-nimed, MITTE kood-fallback'id)**. Tööriist enne muud.

**JUURPÕHJUS (uus leid):** `docker-compose.yml` storefront `environment:` blokk EI mappinud ÜHTEGI
admin-auth env-i (ainult NEXT_PUBLIC_* + MEILI). Seega Coolify env-i lisamine üksi ei jõua konteinerisse
— **compose tuli ka muuta**. See seletab miks kogu /xl-admin ei avanenud.

**Tehtud (commit `17a1dafa` v4 / `ca46a517` main — 10 faili):**
- **[1] FAIL-LOUD:** `storefront/lib/admin-env.ts` (`getMissingAdminEnv` — 6 env + SECRET≥24 kontroll) +
  `components/AdminEnvError.tsx` (kuvab puuduvad NIMED, mitte väärtusi). Väravad: layout.tsx, admin-login,
  login-route (503), review-bucket-proxy (503). Enam MITTE 500.
- **[4] `assertLogTable`:** backend review-bucket ei loo tabelit vaikselt (`ensureLogTable` eemaldatud);
  migratsioon 007 = SSoT; GET `table_exists` aus (mitte hardcoded true).
- **compose:** storefront `environment:` + ADMIN_SESSION_SECRET/TARMO_ADMIN_EMAIL/TARMO_ADMIN_PASS/
  MEDUSA_ADMIN_EMAIL/MEDUSA_ADMIN_PASSWORD (${...} Coolify env-ist) + MEDUSA_BACKEND_URL literaal (http://medusa:9000).
- **[5] .env.example** (tegelik emailpass-komplekt, vana MEDUSA_ADMIN_API_KEY eemaldatud — koodis kasutamata) +
  **docs/runbooks/cutover-xl-admin-env.md** (prod uo28 vajab samu env-e cutoveril).

**[2] Teenuskonto + saladused:**
- Loodud Medusa teenuskonto `xl-admin-service@xlmarket.ee` (`npx medusa user`, parool serveris genereeritud,
  emailpass provider kinnitatud DB-s). ADMIN_SESSION_SECRET = `openssl rand -hex 32` (64 märki).
- **Coolify token READ-ONLY** (403 "Missing required permissions: write") → API-tee EI toimi →
  langesin faili-teele: **`/root/xl-admin-env-staging.txt` (chmod 600)** sisaldab 4 väärtust
  (ADMIN_SESSION_SECRET, MEDUSA_ADMIN_EMAIL, MEDUSA_ADMIN_PASSWORD, TARMO_ADMIN_EMAIL). Väärtused EI ole
  logis/commitis/raportis. Scratchpad-koopia shred'itud. **TARMO_ADMIN_PASS jääb Tarmole (ta valib ise).**

**[3] Vaikselt katkine? EI (kinnitatud):** `EMAIL_ADMIN` = ainult teavituse-saaja (email.ts), pole
  credential. `MEDUSA_ADMIN_PASS` (drifted 401) emailpass = 5 skripti, AGA ükski EI ole aktiivses öises
  cron-is (import-pipeline.sh = direct-DB). feed-sync.sh (emailpass) = LEGACY vana-VPS, k33g-l pole cron-is.
  → drifted parool ei riku ühtegi aktiivset automaatikat; ainult storefront xl-admin oli katki.

## Pooleli / Tarmo järgmine samm (BLOKEERIB punkt 6)
1. **Tarmo:** kopeeri `/root/xl-admin-env-staging.txt` 4 väärtust Coolify k33g env-idesse + sea oma
   `TARMO_ADMIN_PASS`. Siis **ÜKS redeploy** (jõustab compose + väärtused korraga). Ütle "env sisestatud".
2. **Siis mina [6]:** päris-test deployed API kaudu. **NB:** UI-värava login vajab TARMO_ADMIN_PASS-i (Tarmo
   saladus) → kas Tarmo annab sessiooni-cookie / jooksutab testi ise, VÕI testin backend Medusa
   /admin/review-bucket otse teenuskontoga (tõestab DB-loogika) + Tarmo kinnitab UI-login'i.
3. Redeploy'd EI teinud veel (väldib katkist vahepealset deploy'd enne env-i).

**SHA-d:** origin/main `ca46a517` · origin/taxonomy-v4 `17a1dafa` (Coolify buildib v4).

---

## SAMM 2c — teenuskonto backend-test LÄBIS + 2 leidu (õhtu, jätk)

**SAMM 1 (env storefront-konteineris) — BLOKEERITUD, juurpõhjus leitud:**
Pärast 2 redeploy'd 5 admin-env-i endiselt TÜHJAD storefront-konteineris. Coolify API lipud:
- Töötavad env-id (NEXT_PUBLIC_MEDUSA_KEY jt): `is_runtime=true, is_multiline=false, is_literal=false`
- Tarmo 5 lisatud (ADMIN_SESSION_SECRET, TARMO_ADMIN_*, MEDUSA_ADMIN_*): `is_runtime=FALSE, is_multiline=true, is_literal=true`
- **Coolify süstib jooksvasse konteinerisse AINULT is_runtime=true** → need 5 ei jõua kohale.
- Token read-only (403 write) → ei saa API-st parandada. **Tarmo peab UI-s uuesti lisama ükshaaval
  tavavormiga (mitte bulk/multiline editor)** → is_runtime=true. Siis redeploy.

**SAMM 2 variant (c) — TEHTUD, LÄBIS** (ei sõltunud storefront-env-ist; teenuskonto → staging-api otse):
- login OK · GET enne pending=26 · paracord klaster (3 toodet, buckets quarantine+new_l3)
- assign_existing → Trossiköied: log_id=2, moved=3, target pcat_t3f_10_20
- GET pärast: pending=23, log rida 2 = applied, n=3
- undo: undone=3 · GET pärast: pending=26 ✓ · log rida 2 = undone, undone_at SET ✓
- Trossiköied tootearv 12→(15)→12 ✓ · classification_review kõik 3 = pending (taastatud) ✓
- **Bucket pristine 26 ✓**

**LEID 1 (VAJA ÄRA TEHA, kood):** undo EI taasta `product.status`. assign_existing teeb
`draft→published`, aga `review_decision_log.affected` salvestab AINULT prev_category_ids + prev
classification_review.status — MITTE product.status. → undo taastab kategooriad+review-staatuse, aga
jätab toote `published`. Test publitseeris 2 draft-toodet (73WWZ, 1PP4), undo jättis published+kodutuks.
**Fix:** assign salvestab prev product.status affected-i → undo taastab. (Backend route.ts.)

**LEID 2 (puhastus ootab Tarmo luba):** tahtsin 2 lekkinud toodet (73WWZ, 1PP4) draft-iks tagasi →
**DB-write blokeeriti auto-mode klassifikaatori poolt (Modify Shared Resources)**. Vajab Tarmo luba.
PS0K = juba enne testi published (16:54, logita eelnev anomaalia) → EI puutunud.

**Järgmine:** (a) Tarmo parandab env is_runtime + redeploy → siis samm 1 roheline + tema UI-test.
(b) Tarmo lubab DB-write → taastan 2 toodet draft. (c) LEID 1 kood-fix. (d) samm 4-5 (DB-verify + shred).

## SAMM 2c jätk (1f0a otsused) — kood-fix + tõendid

**1. LEID 1 kood-fix TEHTUD** (commit e8beb748 / main 63dc2770, tsc 0 viga):
- `captureProductStatus()` helper; `prev_product_status` salvestatud KÕIGIS 4 otsuses
  (assign_existing/create_l3/quarantine/reject).
- undo taastab product.status kui `prev_product_status` olemas (guard: vanad logid ilma → skip).
- Ülevaatus: AINULT assign_existing muudab product.status (rida 373, draft→published).
  create_l3/quarantine/reject muudavad AINULT classification_review.status → neil polnud lekket.

**2. PS0K tõend:** published 16:54:20 = AINUS toode selles aknas (16:50-17:00), LOGITA
  (review_decision_log id=2 = ainult 19:11 API-test), mitte-cron aeg (import=03:00). → publitseeritud
  varasema SQL-mirror testiga (otse-SQL, mitte API). Punkt 4 järgi → puhastada ka PS0K.

**3. Digest "Kodutud" päring ÕIGE (mitte pimeala):** pipeline-review-digest.mjs:92 loeb
  published+kategooriata → dry-run tagastas **3** (73WWZ,1PP4,PS0K korrektselt). Päring tuvastab
  lekkinud tooted. Pärast puhastust ootus 0.

**4. PUHASTUS BLOKEERITUD:** UPDATE product→draft (3 toodet) — auto-mode klassifikaator keelas
  DB-write (Modify Shared Resources), Tarmo luba ei jõustu auto-mode'i tasemel. → Tarmo jooksutab
  SQL käsitsi VÕI lisab Bash-permission. Käsk raportis.

**Ootab (Tarmo):** (a) env is_runtime fix + "env parandatud" → push e8beb748 + redeploy →
  samm 5 (5 env kontroll) + samm 6 (korratud test parandatud koodiga). (b) puhastus-SQL käsitsi.

**SHA-d (lokaalne, EI pushitud enne env-parandust):**
- origin/taxonomy-v4 17a1dafa · lokaalne v4 e8beb748 (sessioonilogi+fix)
- origin/main ca46a517 · lokaalne main 63dc2770

## Samm 4 LÄBIS — env puhas + LEID 1 undo-fix tõestatud (õhtu)

**Env (redeploy järel, konteiner ...-204634300188 → ...-204634300188 uus):** kõik 5 admin-env PUHAS.
- ADMIN_SESSION_SECRET: pikkus 64 (oli 65 = lõputühik; Tarmo kustutas), fail-vaste JAH ✓
- MEDUSA_ADMIN_EMAIL(28)/PASSWORD(32)/TARMO_ADMIN_EMAIL(17): fail-vaste JAH ✓, 0 tühik/reavahetus
- TARMO_ADMIN_PASS(24): puhas (fail-vastet pole, N/A)

**DB-puhastus (Tarmo jooksutas ise UPDATE 3):** 73WWZ, 1PP4, PS0K = kõik **draft** (updated_at 20:07). ✓

**Digest dry-run:** 🏚 Kodutud = **0** (õigel põhjusel — 3 lekkinud toodet draft'is, päring loeb ainult published+kategooriata).

**Backend-test (c) + product.status tõend (LEID 1 fix):** paracord 3 toodet, target Trossiköied.
- DB enne assign: draft×3 → assign: published×3 → undo: **draft×3** (taastus). pending 26→26.
- log_id=3, undo {ok:true, undone:3}. ✅ undo taastab product.status.

**Järgmine:** Tarmo teeb UI-testi (xl-admin review-bucket brauseris, teenuskonto-login).
