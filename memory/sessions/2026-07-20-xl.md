# Sessioon 2026-07-20 — XL storefront (STAGING k33g / taxonomy-v4)

## Kokkuvõte
Kategooria-lehe vasak-filter + "temporarily unavailable" tõrke ahel. Kõik commits
mõlemal harul (main + taxonomy-v4, worktree-muster).

## Tehtud (kronoloogia, commit-id main)
1. **CategoryTreeNav** (52a276fb) — ehitatud kogu-puu-lahti vasak-nav. **Tarmo lükkas tagasi** (ei sobinud).
2. **Otsingu-stiilis KATEGOORIAD-filter** (0cdf52de) — `categories` facet, ei tühjene. AGA:
3. **Filter-klõps ei kitsendanud** (ae1bb88a) — JUUR: `/api/products` allowlist aktsepteerib AINULT handle-välju + `isSafeHandleToken` (nimi tühiku/äöü → maha). Lisaks `(a OR b)` ümbris katkestas parseFilter regex'i. FIX: `categories`-nimi → `taxonomy.ancestors`-handle, iga valik ERALDI elemendina (mitte OR-sulg). Valideeritud live: Ehitus 1024 → klõps Uksed 200 ✓.
4. **Ribbons id-400** (34f8db01) — "Hiljuti vaadatud" pärib Meili OTSE `id IN [...]`, aga `id` pole filterable → 400 → riba tühi. FIX: `id IN` → `handle IN` (hook lisatud `handles` return).
5. **ProductGrid retry** (9e0462ba) — "temporarily unavailable" JUUR.

## JUUR: "temporarily unavailable" = deploy-swap aken (INFRA, mitte kood)
**Deploy-monitor TABAS LIVE (16:36):** stack-redeploy → storefront tuleb üles ENNE
Meili't → `/api/products` = **503 ~6s** (16:36:43–49) → grid "unavailable", siis taastub.
13 min enne+pärast = 100% 200. Välistatud (kõik): semaphore (fix olemas) · auditd (pole
uid-1001 reeglit, backlog 0) · ressurss (load 0.26) · grid ei kutsu Medusat (0 hinnata-toodet)
· service-worker puudub · `/api/health` staatiline · server 0% (120 paralleelset) · fresh
Playwright 0% (cold+nav+re-nav). Tarmo QA-b kohe redeploy järel → tabab akna.

**FIX (kood):** ProductGrid retry backoff [1.5,3,4.5]s = 3× (~9s katvus) transient-tõrkel
(5xx/net-reject), 4xx ei korrata → grid taastub ise.
**JUUR-FIX (infra, Tarmo/Coolify):** rolling/zero-downtime deploy VÕI Meili-healthy-enne-storefronti.

## Gotcha'd (uued)
- `/api/products` + `/api/search` allowlist = AINULT handle-väljad (`taxonomy.ancestors` jne),
  `isSafeHandleToken` = `/^[a-z0-9][a-z0-9_-]{0,127}$/i` (ei tühikut/äöü). Kategooria-nimed
  EI sobi filtriks — kasuta handle'it.
- `parseFilter` loeb AINULT lihtsaid `väli = "väärtus"` paare (";"=AND). Sulg/OR → maha kukub.
- Meili `id` EI ole filterable; `handle` ON. Brauseri-otse-Meili päringutes kasuta handle'it.
- Coolify k33g = 1 konteiner/teenus, EI rolling → deploy-swap aken ~6s (storefront enne Meili't).
- Meilis 0 toodet `price<=0` → enrichMissingPrices (Medusa) ei käivitu kunagi → grid puhas-Meili.

## Tööriistad (scratchpad, ei git)
- Playwright-diag (kliendipoolne repro) + deploy-monitor.sh (poll 1s, re-resolvib konteineri).

---

## Sessiooni 2. pool (õhtu) — nav, Outlet, cleaner, auditid

### Nav / mega-menüü
- **Mega-menüü akordion** (90709c47) → Tarmo lükkas tagasi (tahtis vana hover-kaskaadi jätta).
- **Taasta vana ☰ Kategooriad + lisa Kategooriapuu-link** (e50749a0) — vana 677-rea hover-kaskaad taastatud; uus CategoryTreeMenu (akordion-dropdown, taaskasutab CategoryTreeNav) ülariba + footer.
- **Eemalda ülariba Kategooriapuu** (4c13b093) — dubleeris ☰ Kategooriad; footeris jääb.

### Outlet (26. main) — MUSTER
- **Outlet 26. main + esimene toode** (8571b347) — v4-outlet (pcat_v4_l26), tuulik-koopia -10% (€124.19), SKU -OUTLET, "(rikutud pakend)", laoseis 1, metadata→originaal. Originaal PUUTUMATA.
- **KRIITILINE DB-fix:** staging `product_variant_price_set` puudus UNIQUE (price_set_id,variant_id) → tooteloomine oli KATKI (createProductsWorkflow upsert fail). Replikeeritud prod-ist (`fix-pvps-unique-migrate.sql`). **Süsteemne — vaja iga tooteloomiseks staging'is.**
- **Outlet L2 + ikoon** (8682234a) — Tarmo valik A: 2 L2 (Rikutud/Avatud pakend), toode → Rikutud pakend L2. V3_ICON BadgePercent. **NB:** V3_ICONS on legacy-slug-põhine → teised v4-mainid ei saa ikooni (lünk).
- Muster kinnitatud: AINULT Outlet-kategoorias (üks kodu); topelt-kuvamine tüübi-puus = Phase-2. Raport: `reports/outlet-muster.md`.
- **Esilehe diagnoos:** Outlet ON esilehel (getVisibleL1 SSoT), aga tile paistis tühi (0 L2 + pole ikooni) → parandatud L2+ikoon.

### Loomis-meetod (õppetund)
- Toode luuakse **`medusa exec` + createProductsWorkflow** (framework, /app-st). Admin-API ei töötanud (staging admin-parool ≠ MEDUSA_ADMIN_PASS).
- genyM.mjs MAINS = HARDCODED 25(→26) L1; L2/L3 DB-st mpath järgi. Uus main → lisa MAINS + DB (pcat_v4_lN + taxonomy_node_meta level).

### VEVOR-cleaner rakendatud (b3933d7c)
- `sanitized_rich_description` puhastus: 363 UPDATE (238 boilerplate/CSS-saba ära + 125 CSS-leak-prügi tühjenes). Meili 17106. 0 sisukadu, 0 pilte, bränd/title/müügipunktid alles. Tööriist git-i: `scripts/vevor-description-cleaner.mjs`. Backup: `reports/backups/k33g-PRE-CLEANER-rich-*.ndjson`.

### Audit (read-only) — tõlke-eelsed lünkad
- **Juhendid:** manual_urls 78.5% (13720/17469), batch OSALINE (21.5% puudu, sh tuulik). route.ts renderdab (PDF-lingid). Mitte tõlke-blokeeriv.
- **Tehniline info:** struktureeritud spec-väli PUUDUB. dimensions 95% + weight 95% olemas AGA EI RENDERDATA. Specid = ebausaldusväärne parseSpecs(description). Proof/plaan puudub. **Suurim lünk.** Kiirvõit: renderda dimensions/weight (ei vaja tõlget).

## Homseks (stardipunkt)
- Tehniline info: (a) renderda dimensions/weight (kiirvõit), VÕI (b) ehita struktureeritud spec-ekstraktsioon + proof.
- Manuals-batch lõpetamine (21.5% puudu) — valikuline.
- V3_ICONS v4-mainide ikoonid (kõik 26, mitte ainult Outlet) — kui järjekindlust soovib.
- Cutover ootab endiselt (main v4-canonical).

## Uued gotcha'd
- Staging DB link-tabelitel võib puududa UNIQUE-constraint (prod'il on) → Medusa workflow upsert fail. Kontrolli `product_variant_price_set` jt link-tabelid prod vs staging.
- Medusa tooteloomine: `medusa exec` /app-st (import resolvib node_modules); admin-API parool võib driftida.
- V3_ICONS = legacy-slug-põhine, EI v4-handle → v4-mainidel pole ikoone.
- genyM MAINS hardcoded; scripts/ + v4-staging/ on main-only (ei taxonomy-v4 harul).
- Konteinerite nimed muutuvad iga redeploy'l → `docker ps | grep '^db-k33g'` re-resolvi.
