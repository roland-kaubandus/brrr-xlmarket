# XL sessioonilogi — 2026-03-28

## Mis tehti
### 1. WO-XLM-001 — Infrastruktuur (DONE)
- Repo loodud: oitmaaristo/brrr-xlmarket (private)
- CLAUDE.md, settings.json, docker-compose.yml
- PostgreSQL 16 (port 5435) + Redis 7 (port 6380) Docker containers
- Medusa.js 2.0 backend (port 9001) installitud ja käivitatud
- Next.js 15 storefront (port 3030) builditud ja käivitatud
- nginx reverse proxy (port 8090) seadistatud

### 2. WO-XLM-002 — Store konfiguratsioon (DONE)
- Store nimi: XLMARKET
- Regioon: Eesti (EUR)
- Käibemaks: 22% (ee-vat-22)
- 11 peakategooriat eesti keeles
- Publishable API key loodud ja sales channel'iga lingitud
- Admin kasutaja: tarmo@xlmarket.eu

### 3. WO-XLM-003 — XLSX Feed Import (DONE)
- import-feed.mjs script loodud (Node.js + exceljs)
- 60 VEVOR L1 kategooriat mapitud 11 ET kategooriasse
- 14 280 toodet imporditud, 0 viga
- 10 719 toodet laos (published), ülejäänud draft
- Hinnad: algne * 1.15 = lõpphind sentides

### 4. WO-XLM-001/002 parandused (sessiooni algus)
- Eemaldatud eraldi admin teenus docker-compose'ist (Medusa 2.0 admin on /app-s)
- Parandatud medusa-config.js portid (9001) ja CORS seaded
- Multi-stage Dockerfile, NODE_ENV=production
- nginx: lisatud /auth/ proxy
- Healthcheck'id medusa/storefront teenustele (wget-ga Alpine jaoks)
- Env muutujate nimed konsistentseks (NEXT_PUBLIC_MEDUSA_KEY jne)
- Review + test + gatekeeper läbitud

### 5. WO-XLM-005 — Storefront lehed (DONE)
- Avaleht: hero + 11 kategooriat + 12 uusimat toodet
- Kategooria leht: tooted + hinna-/sort-filtrid + pagination
- Toote leht: pilt, hind, laoseis, kirjeldus (HTML), lisa korvi
- Otsing: toodete otsimine + pagination
- Uus: CategoryFilters komponent (min/max hind, sort)
- Uus: toote kirjeldus renderdab HTML-i korrektselt (dangerouslySetInnerHTML)
- Uus: serveripoolne hinnafiltreerimine kategooria lehel

### 6. WO-XLM-006 — Checkout (DONE)
- /tellimus leht: kliendi andmed, aadress, tarneviis, kokkuvõte
- Cart → Order flow: address → shipping → complete
- Tellimuse kinnitus ekraan

### 7. SEO ja feedid (sessiooni jätk)
- osta.ee XML feed: 10,719 toodet, scripts/generate-osta-feed.mjs
- sitemap.xml: 10,733 URLi, scripts/generate-sitemap.mjs
- SEO meta tags: title, description, OG, Twitter Card kõigile lehtedele
- robots.txt koos sitemap viitega
- nginx rate limiting: store 20r/s, auth 5r/s, storefront 30r/s
- error.tsx eestikeelne vealeht
- Cron: feedid + sitemap iga 4h

## Otsused
- Medusa.js 2.0 (mitte custom FastAPI) — e-poe funktsionaalsus out-of-the-box
- Port 8090 nginx jaoks (port 80 on teiste saitide pärast hõivatud)
- Tooted jooksevad host'il (mitte Dockeris) dev-režiimis
- XLSX feed alla laetud data/feeds/ kausta, gitist välja jäetud
- Medusa 2.0 Store API ei toeta hinnafiltrit — kasutame serveripoolset filtreerimist (fetch 500 + filter)
- Medusa 2.0 admin on built-in (/app), eraldi admin teenust pole vaja
- osta.ee feed: max 60 tähemärki pealkirjale, 300 kirjeldusele
- Otsing/cart/checkout noindex — ei indekseerita

## Probleemid ja lahendused
- Medusa otsib medusa-config.js (mitte .ts) — lõin JS versiooni
- next.config.ts i18n ei toeta App Router — eemaldasin, i18n hiljem [locale] kaudu
- Node.js URL localhost -> IPv6 ECONNREFUSED — muutsin 127.0.0.1-ks
- Handle'ites topelt sidekriipsud -- — lisasin replace(/-{2,}/g, "-")
- Bash history expansion muudab ! märki paroolides — kasuta JSON faili + -d @file
- Docker Alpine ei sisalda curl — kasuta wget healthcheck'ides
- nginx 403 data/feeds — /home/brrr puudus o+x — parandatud chmod'iga

### 8. Bug fix sessiooni jätk (2026-03-28 õhtupoolik)
Desktop Claude'i audit leidis 9 bugi (XLM-17..XLM-25). Kontrollisin kõiki:
- XLM-17 (ostukorvi leht puudub): JUBA PARANDATUD — storefront/app/ostukorv/page.tsx eksisteerib ja on täisfunktsionaalne
- XLM-19 (laoseisu kontroll): PARANDATUD — eemaldatud unreachable kood, lihtsustatud loogika (variant ? "Laos" : "Pole saadaval")
- XLM-20 (hardcoded credentials): JUBA PARANDATUD — kõik env vars'idest (NEXT_PUBLIC_*)
- XLM-21 (kategooriate täpitähed): JUBA PARANDATUD — API tagastab õiged eesti nimed (ö, ä, ü)
- XLM-23 (cart API validatsioon): JUBA PARANDATUD — validation.ts + isValidId/isValidQuantity kõigis route'ides
- XLM-19 läbis review-func (APPROVE), review-ui (APPROVE), tester (10/10 PASS), gatekeeper (APPROVE pärast commit)

### 9. Õiguslikud lehed, GDPR, feedid, SEO (2026-03-28 sessiooni jätk)
- 7 õiguslikku lehte: privaatsus, tingimused, tagastamine, tarne, kontakt, meist, küpsised
- GDPR küpsiste nõusoleku bänner (CookieConsent komponent)
- Footer uuendus: 4 veergu (firma, pood, info, õiguslik) + copyright
- Facebook Commerce XML feed: 10,714 toodet, scripts/generate-facebook-feed.mjs
- Meta Pixel integratsioon (nõusoleku-põhine, laadib ainult pärast "Nõustun kõigiga")
- JSON-LD structured data: Organization (layout), Product + BreadcrumbList (toote leht)
- Interaktiivne toote pildigalerii (klikitavad thumbnailid, aktiivse pildi indikaator)
- HTML sanitizer (lib/sanitize.ts) — XSS kaitse toote kirjelduses
- SVG favicon (amber taust, "XL" tekst)
- robots.txt uuendus (disallow: ostukorv, tellimus, otsing, api)
- Cron uuendatud: Facebook feed iga 4h koos osta.ee ja sitemap'iga
- Review-func: APPROVE (17/18 pass, XSS parandatud)
- Review-ui: APPROVE (a11y parandused tehtud)

### 10. Sessiooni jätk #2 (2026-03-28 16:00+)
Huly issue-de läbitöötamine. Kõik blokeerimata WO-d ja bugid:

**WO-009 (Brändi disain) — DONE:**
- Inter font (next/font/google, latin + latin-ext)
- Brand värvid: Tailwind v4 @theme (brand-50..900, amber-põhised)
- XL|MARKET logo stiil: XL brand-600 + MARKET gray-900
- Kõik amber→brand viited ühtlustatud 21 failis

**WO-010 (Eesti lokaliseerimine) — DONE:**
- UI juba 100% eesti keeles (varasemast sessioonist)
- i18n framework pole vajalik (ainult eesti keel)

**XLM-18 (Tootenimed inglise keeles) — DONE (taustal jookseb):**
- translate-products.mjs script loodud (google-translate-api-x)
- EN→ET tõlge ~0.9 toodet/sek, 14K toodet
- Originaalnimed salvestatud metadata.original_title alla
- Käivitatud taustal, lõpetab ~5h pärast

**WO-008 (Email teavitused) — DONE:**
- Nodemailer SMTP transport (töötab iga SMTP serveriga)
- Eestikeelsed HTML email mallid (XLMARKET bränding)
- Subscriber: order.placed → tellimuse kinnitus + admin teavitus
- Subscriber: order.fulfillment_created → saatmise teavitus
- Graceful fallback: ilma SMTP_PASSWORD'ita logib konsooli

**WO-015 (CMS sisublokid) — DONE:**
- JSON-põhine CMS (data/cms/content.json)
- Admin API: GET/POST /admin/cms
- Store API: GET /store/cms
- Komponendid: PromoBanner, AnnouncementBar
- Avaleht kasutab CMS sisu dünaamiliselt

**WO-011 (Domeen, SSL, nginx) — DONE:**
- Production nginx config (SSL, gzip, HSTS, security headers, static caching)
- HTTP-only fallback config
- Automaatne SSL setup script (certbot)
- DNS: xlmarket.eu viitab vale IP-le (85.194.202.138 vs meie 65.109.86.254)

**XLM-22 (systemd teenused) — DONE:**
- systemd unit failid: xlmarket-medusa.service, xlmarket-storefront.service
- Auto-restart, journald logging, EnvironmentFile support
- Install script: scripts/install-services.sh

**WO-016 (Turundusplaan) — DONE:**
- Turundusplaan dokument (docs/turundusplaan.md)
- Google Ads, Facebook Ads, osta.ee, SEO, email marketing
- KPI-d, eelarved, ajakava

## Pooleli / TODO
- WO-XLM-007: Montonio maksed — BLOKEERITUD (API keys puuduvad .env-st)
- DNS: xlmarket.eu A record peab viitama 65.109.86.254 peale
- SSL: certbot käivitamine pärast DNS muutust (sudo bash scripts/setup-ssl.sh)
- SMTP: SMTP_PASSWORD puudub .env-st (emailid logitakse konsooli)
- Meta Pixel: NEXT_PUBLIC_META_PIXEL_ID vajab seadistamist
- systemd install vajab sudo (sudo bash scripts/install-services.sh)
- XLM-24: CDN blocked images — low priority, jäetud vahele
