# 2026-04-12 session9

Mis tehti:
- Eemaldasin locale loading-vaatest topelt sticky headeri, mis tekitas lehtede vahetamisel hüpet ja tõmblemist.
- Muutsin root layouti body flex-col + min-h-screen mudelile ja locale layouti põhiala flex-1 peale.
- Lisasin globaalse `scrollbar-gutter: stable`, et scrollbar ei muudaks lehe laiust route-vahetusel.

Kontroll:
- `npm --prefix storefront run build` läks edukalt läbi.
- `next start -p 3030` taaskäivitatud uue buildiga.

Pooleli / TODO:
- Kui kasutaja näeb endiselt hüpet, uurida järgmise sammuna headeri search-bar placeholderi animaatsiooni ja locale switcheri hydrationit.

Failid:
- `storefront/app/[locale]/loading.tsx`
- `storefront/app/[locale]/layout.tsx`
- `storefront/app/layout.tsx`
- `storefront/app/globals.css`
