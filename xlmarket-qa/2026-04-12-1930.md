# XLMarket QA Raport — 2026-04-12 19:30

## Kontrollitud
- Kogu storefront kodebaas: kõik komponendid ja app lehed
- Otsiti ingliskeelseid stringe grep + käsitsi lugemine

## Parandatud selles tsüklis (11 faili, ~60 stringi)

### Komponendid (6 faili):

1. **CartSlideOver.tsx** — 10 stringi tõlgitud:
   - "Shopping Cart" → "Ostukorv" (header + aria)
   - "Close" → "Sulge" (aria)
   - "Your cart is empty" → "Ostukorv on tühi"
   - "Add products to start shopping" → "Lisa tooteid..."
   - "Continue Shopping" → "Jätka ostlemist" (2x)
   - "pcs" → "tk" (kogus)
   - "Total" → "Kokku"
   - "View Cart" → "Vaata ostukorvi"

2. **WishlistPage.tsx** — 5 stringi:
   - "Wishlist" → "Soovinimekiri"
   - "Wishlist is empty" → "Soovinimekiri on tühi"
   - "Click the heart icon..." → eestikeelne juhis
   - "Browse Products" → "Sirvi tooteid"
   - "Remove from wishlist" → "Eemalda soovinimekirjast" (aria)

3. **RecentlyViewed.tsx** — 1 string:
   - "Recently Viewed" → "Hiljuti vaadatud"

4. **ProductReviews.tsx** — 5 stringi:
   - "Reviews" → "Arvustused"
   - "Write a Review" → "Kirjuta arvustus"
   - "Thank you!" → "Aitäh!"
   - "Your review has been submitted..." → eestikeelne kinnitus
   - "No reviews yet..." + "Be the first..." → eestikeelsed

5. **SubcategoryGrid.tsx** — 4 stringi:
   - "Product Groups" → "Tootegrupid"
   - "products" → "toodet" (locale en→et)
   - "Hide" / "View all product groups" → eestikeelsed
   - "Clear filter" → "Tühjenda filter"

6. **NavCartButton.tsx** — 1 string:
   - "Shopping Cart" → "Ostukorv" (aria)

### App lehed (5 faili):

7. **not-found.tsx** (root) — 4 stringi:
   - "Page Not Found" → "Lehte ei leitud"
   - aadress muutunud tekst → eesti keelde
   - "Back to Home" → "Avalehele"
   - "Browse Categories" → "Sirvi kategooriaid"

8. **[locale]/not-found.tsx** — 2 stringi:
   - "Page Not Found" + kirjeldus → eesti keelde

9. **[locale]/login/page.tsx** — 7 stringi:
   - "Sign In" → "Logi sisse" (pealkiri + nupp)
   - "Don't have an account? Create one" → eesti keelde
   - "Email" → "E-post", "Password" → "Parool"
   - placeholder'd eesti keelde
   - "Login failed" → "Sisselogimine ebaõnnestus"

10. **[locale]/register/page.tsx** — 9 stringi:
    - "Create Account" → "Loo konto" (pealkiri + nupp)
    - "Already have an account? Sign in" → eesti keelde
    - "First Name" → "Eesnimi", "Last Name" → "Perekonnanimi"
    - "Email" → "E-post", "Password" → "Parool"
    - placeholder'd ja veateated eesti keelde
    - Privaatsuspoliitika tekst eesti keelde

11. **[locale]/account/page.tsx** — 14 stringi:
    - "My Account" → "Minu konto"
    - "Welcome back" → "Tere tulemast tagasi"
    - "Sign Out" → "Logi välja"
    - "Profile" → "Profiil", "Addresses" → "Aadressid"
    - "Name" → "Nimi", "Email" → "E-post", "Phone" → "Telefon"
    - "Default shipping" → "Peamine tarneaadress"
    - "No saved addresses yet" → eesti keelde
    - "Order History" → "Tellimuste ajalugu"
    - "Order #" → "Tellimus #"
    - Kuupäev en-GB → et-EE locale
    - "No orders yet" + "Start shopping" → eesti keelde
    - "Loading..." → "Laadimine..."

## Teadaolevad probleemid (jäävad)
- KRIITILINE: VPS vajab redeploymenti — kõik tõlked on repos aga mitte live
- KESKMINE: L2/L3 subcategory nimed inglise keeles — Medusa data tasand
- KESKMINE: Breadcrumb kategooria nimed inglise keeles — Medusa data
- KESKMINE: Mõned tootekirjeldused inglise keeles — VEVOR feedist
- MADAL: xlmarket.ee SSL sertifikaat puudub
- MADAL: Tarne/tagastamise info lehed võivad sisaldada ingliskeelset sisu

## Statistika
- Kontrollitud faile: 20+ (kõik komponendid + app lehed)
- Leitud vigu: ~60 ingliskeelset stringi 11 failis
- Parandatud: 11 faili, ~60 stringi → eesti keelde
- Kogu parandusi viie tsükli peale: 95+ parandust 30+ failis
