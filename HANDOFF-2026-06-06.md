# XLMARKET — Handoff (2026-06-06 → järgmine sessioon)

> Lühike üleandmine. Täisdetailid: `memory/sessions/2026-06-06-xl.md`. Live: xlmarket.ee (Coolify, 65.21.126.235).

## 🔴 PRIORITEET 1 — Cart async-stall (root cause, profiling)

**Sümptom:** Medusa cart-loomine (POST /store/carts) vahelduvalt **5-12s** (http-log duration ~5497ms), ~pooled kiired (0.1s). Mõjutab checkout'i + Montonio makset.

**Stopgap LIVE** (töötab, aga band-aid): `storefront/lib/medusa-proxy.ts` timeout 3s→12s + `app/api/cart/route.ts` 2×13s retry. Tulemus: 6/8 õnnestub (1-9s), 2/8 ikka >26s. 504-d kadusid.

**VÄLISTATUD (ära korda):** DNS (lookup <8ms), pg-pool (6 üh, 0 leak), redis-churn (stabiilne age 633s), conntrack (1%), base-image (värske alpine+debian node → connect <10ms), autoSelectFamily (`--no-network-family-autoselection` deployed, internalConnect-vead 0), Montonio (aeglane ka ilma), mailcow-võrk (lahutamine ei aidanud), host-võrk (coolify-proxy→medusa 0ms).

**SUUND:** medusa cart-**WORKFLOW ootab ~5s async'i** — event-loop VABA (/health + /store/regions kiired). Tühi cart (0 toodet) = ainult DB-insert + region-link → 5s absurdne. Kahtlus: **Medusa 2.x inmemory workflow-engine polling-latents** VÕI cart-workflow async-step (payment-collection init / tax automatic_taxes=true).
**Järgmine samm:** async_hooks / workflow step-timing instrumenteerimine. Eraldi fokuseeritud sessioon.

## 🟡 PRIORITEET 2 — Cloudflare proxy (oranž pilv)

- **NS delegeeritud ✅** (edward + penny.ns.cloudflare.com, globaalselt levinud).
- **Proxy HALL 🟡** — autoriteetne NS (edward/penny) tagastab ikka origin IP 65.21.126.235. A-rekord on "DNS only", mitte "Proxied". Kasutaja proovis 3× dashboardis, ei jää oranžiks.
- **Mul POLE Cloudflare API-tokenit** (otsisin, puudub). Kasutaja andis zone ID `04661523023085812102ba1064d79bd375` (34 märki — CF zone ID on 32, **kontrolli üle**).
- **Vaja:** kasutajalt CF API-token (Zone.DNS:Edit + Zone:Read) → siis saan API kaudu `PATCH dns_records {proxied:true}` + activation_check. VÕI kasutaja kustutab+loob A-rekordi Proxied'ina dashboardis. Sait töötab praegu (SSL OK), proxy = CDN/kaitse lisaväärtus, mitte blokeerija.

## 🟢 Sel sessioonil TEHTUD + LIVE
salon-spa fix · breadcrumb ET · **Montonio hostname** (sandbox-api→sandbox-stargate, oli täiesti vale) · legal-lehed ET · UI ET-tõlge (~200 str) · MEDUSA_BACKEND_URL · uptime-kuma (xlmarket.ee+admin) · restore-test ✅ · maintenance-konteiner eemaldatud · **32 toodet kategoriseeritud** (leitavus 99.97%).

## 📋 Järgmised prioriteedid (pärast cart+CF)
1. **vevor-to-v3.json l1_defaults sünk** praeguste 18 L1-handle'ga (STALE → uued tooted ei kategoriseeru). ~6 toodet veel l1_slug NULL.
2. **Feed-sync Coolify Scheduled Task** registreerimine — skriptid valmis+deployed (`backend/scripts/feed-sync-bulk.sh`), vaja Coolify task `0 */4 * * *` → `bash /app/scripts/feed-sync-bulk.sh` + käsitsi 1× test.
3. **Powermat:** powermat-to-v3.json mapping + pipeline rewire + import.
4. **Faas C** (900 kat 1-4 tootega) — PÄRAST Powermati (arvud nihkuvad).
5. Montonio sandbox e2e test (kui cart-stall lahendatud).
6. Post-launch: FAAS 2 live-UI, legacy 629 DB-kat koristus.

## ⚙ Deploy-meelespea (KORDUB igal deploy'l)
1. `curl -H "Authorization: Bearer $(cat /tmp/coolify-token.txt)" "http://localhost:8000/api/v1/deploy?uuid=uo28ovobnflauslqjgxeohl0"`
2. Medusa boot ~8.5 min (healthcheck start-period 600s).
3. **Post-steps:** `docker network connect mailcowdockerized_mailcow-network <uus-medusa>` + Traefik yaml (medusa-admin.yaml + xlmarket-dev.yaml) konteineri-nimed coolify-proxy's.
4. DB/Meili IP-d muutuvad → re-derive `docker inspect`.
