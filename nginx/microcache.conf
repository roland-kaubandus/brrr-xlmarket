# Microcache for Next.js SSR responses
# Caches page responses for 5s to prevent CPU saturation from rapid navigation
# Only caches GET requests, skips API routes and authenticated requests
proxy_cache_path /tmp/nginx-xlmarket-cache levels=1:2 keys_zone=xlmarket_ssr:10m max_size=100m inactive=60s;
