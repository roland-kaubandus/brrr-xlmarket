# Environment
.env
.env.local
.env.production

# Node
node_modules/
.next/
dist/
build/

# Data (raw feeds, not in git)
data/feeds/*.xlsx
data/feeds/*.xml
data/feeds/sitemap.xml.stale*
data/feeds/sync-reports/
backend/data/feeds/*.xlsx
backend/data/feeds/vevor-feed-cache.json

# Reports: track markdown work-reports (dev-workflow), ignore large generated outputs
reports/*.csv
reports/image-gen-*/
reports/spu-split-*

# Backups — large DB/meili dumps, NEVER in git (HANDBOOK §5)
archive/

# Category-icon scraper runtime state (regenerated per run)
data/category-icons/retry-queue.json
data/category-icons/review-results.json
data/category-icons/search-queue-*.json
data/category-icons/search-results-*.json

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Build artifacts
tsconfig.tsbuildinfo
*.tsbuildinfo

# Medusa auto-generated types — regenerated on every `medusa build`.
# Tracking these files causes spurious diffs on every build.
backend/.medusa/types/

# Docker volumes
pgdata/

# Logs
*.log
npm-debug.log*

# Claude Code
.claude/todos/
.claude/history.jsonl
node_modules/
data/feeds/*.xlsx
.dmux/
.claude/worktrees/

# Translation fleet operational artifacts (logs, trackers, batch outputs)
backend/data/translation-batches/

# Tarmo/Risto tööfailid (price lists, oversizes)
storefront/*.xlsx

# Operatsiooni-väljundid (env vars, secrets, raportid)
outputs/

# Backup-dumbid (EI lähe git'i — mälu-reegel)
backups/
reports/backups/
*.bak-stale
reports/credit-outage.state
